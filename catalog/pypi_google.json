{
  "category": "osint",
  "description": "google is a Python library to query Google Search (unofficial / scraper-based).",
  "documentation": {
    "entry": "google",
    "example": "from google import search; [print(r) for r in search('test', num_results=5)]",
    "import": "google",
    "pypi": "https://pypi.org/project/google/"
  },
  "full_name": "google",
  "id": "pypi:google",
  "kind": "python_library",
  "metadata": {
    "category": "osint",
    "entry": "google",
    "example": "from google import search; [print(r) for r in search('test', num_results=5)]",
    "import_name": "google",
    "pip_name": "google",
    "version": "3.0.0"
  },
  "metadata_status": "indexed",
  "name": "google",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "Google search results (unofficial wrapper).",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/google/",
  "tags": [
    "offensive",
    "google",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/google && python3 google.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/google && python3 google.py --help",
    "cd toolboxes/google && python3 google.py --help",
    "cd toolboxes/google && python3 google.py --help",
    "cd toolboxes/google && python3 google.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST google --output $KFIOSA_OUTPUT_DIR/google.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST google --output $KFIOSA_OUTPUT_DIR/google.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}