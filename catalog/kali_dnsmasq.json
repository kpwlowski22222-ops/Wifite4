{
  "id": "kali:dnsmasq",
  "kind": "kali_source_package",
  "name": "dnsmasq",
  "title": "dnsmasq",
  "summary": "Small caching DNS proxy and DHCP/TFTP server - system daemon",
  "description": "Small caching DNS proxy and DHCP/TFTP server - system daemon\nDnsmasq is a lightweight, easy to configure, DNS forwarder and DHCP\nserver. It is designed to provide DNS and optionally, DHCP, to a\nsmall network. It can serve the names of local machines which are\nnot in the global DNS. The DHCP server integrates with the DNS\nserver and allows machines with DHCP-allocated addresses\nto appear in the DNS with names configured either in each host or\nin a central configuration file. Dnsmasq supports static and dynamic\nDHCP leases and BOOTP/TFTP for network booting of diskless machines.",
  "version": "2.93-1",
  "architectures": [
    "all"
  ],
  "homepage": "https://www.thekelleys.org.uk/dnsmasq/doc.html",
  "repository": "https://www.thekelleys.org.uk/dnsmasq/doc.html",
  "source_package": "dnsmasq",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install dnsmasq"
  },
  "packages": [
    {
      "name": "dnsmasq"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package",
    "networking"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use dnsmasq because Small caching DNS proxy and DHCP/TFTP server - system daemon.",
    "When the engagement is in the install phase, use dnsmasq as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair dnsmasq with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `dnsmasq --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Small caching DNS proxy and DHCP/TFTP server - system daemon\nDnsmasq is a lightweight, easy to configure, DNS forwarder and DHCP\nserver. It is designed to provide DNS and optionally, DHCP, to a\nsmall network. It can serve the names of local machines which are\nnot in the global DNS. The DHCP server integrates with the DNS\nserver and allows machines with DHCP-allocated addresses\nto appear in the DNS with names configured either in each host or\nin a central configuration file. Dnsmasq supports static and dynamic\nDHCP leases and BOOTP/TFTP for network booting of diskless machines.",
    "how_to_use": "Install/setup: sudo apt install dnsmasq. Small caching DNS proxy and DHCP/TFTP server - system daemon.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Small caching DNS proxy and DHCP/TFTP server - system daemon.",
    "why_to_use": "Prefer this tool based on upstream description: Small caching DNS proxy and DHCP/TFTP server - system daemon\nDnsmasq is a lightweight, easy to configure, DNS forwarder and DHCP\nserver. It is designed to provide DNS and optionally, DHCP, to a\nsmall network. It can serve the names of local machines which are\nnot in the global DNS. The DHCP server integrates with the DNS\nserver and allows machines with DHCP-allocated addresses\nto appear in the DNS.",
    "usage_sections": [
      "Install with: sudo apt install dnsmasq",
      "Install with: sudo apt install dnsmasq"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
