{
  "id": "kali:libclone-pp-perl",
  "kind": "kali_source_package",
  "name": "libclone-pp-perl",
  "title": "libclone-pp-perl",
  "summary": "module to recursively copy Perl datatypes",
  "description": "module to recursively copy Perl datatypes\nClone::PP provides a general-purpose clone function to make deep\ncopies of Perl data structures. It calls itself recursively to copy\nnested hash, array, scalar and reference types, including tied\nvariables and objects.\n.\nThe clone() function takes a scalar argument to copy. To duplicate\narrays or hashes, pass them in by reference:\n.\nmy $copy = clone(\\@array);    my @copy = @{ clone(\\@array) };\nmy $copy = clone(\\%hash);     my %copy = %{ clone(\\%hash) };\n.\nThe clone() function also accepts an optional second parameter that\ncan be used to limit the depth of the copy. If you pass a limit of\n0, clone will return the same value you supplied; for a limit of\n1, a shallow copy is constructed; for a limit of 2, two layers of\ncopying are done, and so on.\n.\nThis is a pure-perl implementation. For a faster XS-/C-based\nimplementation see the libclone-perl package.",
  "version": "1.08-2",
  "architectures": [
    "all"
  ],
  "homepage": "https://metacpan.org/release/Clone-PP",
  "repository": "https://metacpan.org/release/Clone-PP",
  "source_package": "libclone-pp-perl",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libclone-pp-perl"
  },
  "packages": [
    {
      "name": "libclone-pp-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/Clone::PP.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libclone-pp-perl because module to recursively copy Perl datatypes.",
    "When the engagement is in the install phase, use libclone-pp-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libclone-pp-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libclone-pp-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "module to recursively copy Perl datatypes\nClone::PP provides a general-purpose clone function to make deep\ncopies of Perl data structures. It calls itself recursively to copy\nnested hash, array, scalar and reference types, including tied\nvariables and objects.\n.\nThe clone() function takes a scalar argument to copy. To duplicate\narrays or hashes, pass them in by reference:\n.\nmy $copy = clone(\\@array);    my @copy = @{ clone(\\@array) };\nmy $copy = clone(\\%hash);     my %copy = %{ clone(\\%hash) };\n.\nThe clone() function also accepts an optional second parameter that\ncan be used to limit the depth of the copy. If you pass a limit of\n0, clone will return the same value you supplied; for a limit of\n1, a shallow copy is constructed; for a limit of 2, two layers of\ncopying are done, and so on.\n.\nThis is a pure-perl implementation. For a faster XS-/C-based\nimplementation see the libclone-perl package.",
    "how_to_use": "Install/setup: sudo apt install libclone-pp-perl. module to recursively copy Perl datatypes.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: module to recursively copy Perl datatypes.",
    "why_to_use": "Prefer this tool based on upstream description: module to recursively copy Perl datatypes\nClone::PP provides a general-purpose clone function to make deep\ncopies of Perl data structures. It calls itself recursively to copy\nnested hash, array, scalar and reference types, including tied\nvariables and objects.\n.\nThe clone() function takes a scalar argument to copy. To duplicate\narrays or hashes, pass them in by reference:\n.\nmy $copy = clone(\\@arra.",
    "usage_sections": [
      "Install with: sudo apt install libclone-pp-perl",
      "Install with: sudo apt install libclone-pp-perl"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
