{
  "id": "kali:perl-openssl-defaults",
  "kind": "kali_source_package",
  "name": "perl-openssl-defaults",
  "title": "perl-openssl-defaults",
  "summary": "version compatibility baseline for Perl OpenSSL packages",
  "description": "version compatibility baseline for Perl OpenSSL packages\nA subset of Perl XS module packages expose the OpenSSL binary interface\nto Perl code. This can lead to incompatibilities if these packages are\nlinked against different versions of OpenSSL.\n.\nThis package provides a virtual package \"perl-openssl-abi-x\" that\ncorresponds to the libssl-dev package SONAME it was built against.\nThe packages that need to stay compatible with each other can depend\non this.\n.\nTools are also provided for generating this dependency with minimum\nhassle. See the instructions in README.Debian.",
  "version": "7+b2",
  "architectures": [
    "amd64"
  ],
  "homepage": "",
  "repository": "",
  "source_package": "perl-openssl-defaults (7)",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install perl-openssl-defaults"
  },
  "packages": [
    {
      "name": "perl-openssl-defaults"
    }
  ],
  "commands": [
    {
      "name": "dh_perl_openssl",
      "path": "/usr/bin/dh_perl_openssl",
      "description": "add dependencies required for OpenSSL modules",
      "usage": "dh_perl_openssl [options] [args]",
      "arguments": [
        {
          "flags": [
            "-sequence-perl"
          ],
          "metavar": null,
          "takes_value": false,
          "description": "openssl and omit the \"--with perl_openssl\" part.",
          "default": null,
          "choices": null,
          "raw": "-sequence-perl  openssl and omit the \"--with perl_openssl\" part."
        }
      ]
    }
  ],
  "files": [
    "/usr/bin/dh_perl_openssl",
    "/usr/share/man/man1/dh_perl_openssl.1.gz"
  ],
  "tags": [
    "os-package"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "command_examples": [
    "dh_perl_openssl [debhelper options]"
  ],
  "use_cases": [
    "When you need Kali Linux, use dh_perl_openssl because version compatibility baseline for Perl OpenSSL packages.",
    "When the engagement is in the install phase, use dh_perl_openssl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When you need a concrete invocation, run `dh_perl_openssl [debhelper options]` as documented upstream for dh_perl_openssl.",
    "When chaining Kali Linux tools, pair dh_perl_openssl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `dh_perl_openssl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [
      {
        "name": "-sequence-perl",
        "description": "openssl and omit the \"--with perl_openssl\" part.",
        "source": "docs"
      }
    ],
    "examples": [
      "dh_perl_openssl [debhelper options]"
    ],
    "readme": "dh_perl_openssl - add dependencies required for OpenSSL modules",
    "how_to_use": "Install/setup: sudo apt install perl-openssl-defaults (7). version compatibility baseline for Perl OpenSSL packages.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: version compatibility baseline for Perl OpenSSL packages.",
    "why_to_use": "Prefer this tool based on upstream description: dh_perl_openssl - add dependencies required for OpenSSL modules.",
    "usage_sections": [
      "Install with: sudo apt install perl-openssl-defaults (7)",
      "Install with: sudo apt install perl-openssl-defaults (7)"
    ]
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "local_man"
}
