{
  "id": "kali:postgresql",
  "kind": "kali_source_package",
  "name": "postgresql",
  "title": "postgresql",
  "summary": "object-relational SQL database (supported version)",
  "description": "object-relational SQL database (supported version)\nThis metapackage always depends on the currently supported PostgreSQL\ndatabase server version. It also offers to automatically upgrade the\n\"main\" database cluster to the lastest supported major version.\n.\nPostgreSQL is a fully featured object-relational database management\nsystem.  It supports a large part of the SQL standard and is designed\nto be extensible by users in many aspects.  Some of the features are:\nACID transactions, foreign keys, views, sequences, subqueries,\ntriggers, user-defined types and functions, outer joins, multiversion\nconcurrency control.  Graphical user interfaces and bindings for many\nprogramming languages are available as well.",
  "version": "18+290",
  "architectures": [
    "all"
  ],
  "homepage": "",
  "repository": "",
  "source_package": "postgresql-common (290)",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install postgresql"
  },
  "packages": [
    {
      "name": "postgresql"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package",
    "post-exploit"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use postgresql because object-relational SQL database (supported version).",
    "When the engagement is in the install phase, use postgresql as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair postgresql with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `postgresql --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "object-relational SQL database (supported version)\nThis metapackage always depends on the currently supported PostgreSQL\ndatabase server version. It also offers to automatically upgrade the\n\"main\" database cluster to the lastest supported major version.\n.\nPostgreSQL is a fully featured object-relational database management\nsystem.  It supports a large part of the SQL standard and is designed\nto be extensible by users in many aspects.  Some of the features are:\nACID transactions, foreign keys, views, sequences, subqueries,\ntriggers, user-defined types and functions, outer joins, multiversion\nconcurrency control.  Graphical user interfaces and bindings for many\nprogramming languages are available as well.",
    "how_to_use": "Install/setup: sudo apt install postgresql-common (290). object-relational SQL database (supported version).",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: object-relational SQL database (supported version).",
    "why_to_use": "Prefer this tool based on upstream description: object-relational SQL database (supported version)\nThis metapackage always depends on the currently supported PostgreSQL\ndatabase server version. It also offers to automatically upgrade the\n\"main\" database cluster to the lastest supported major version.\n.\nPostgreSQL is a fully featured object-relational database management\nsystem.  It supports a large part of the SQL standard and is designed\nto be.",
    "usage_sections": [
      "Install with: sudo apt install postgresql-common (290)",
      "Install with: sudo apt install postgresql-common (290)"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
