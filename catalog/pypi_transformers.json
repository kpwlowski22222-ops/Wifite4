{
  "category": "ai",
  "description": "Transformers provides thousands of pretrained models to perform tasks on different modalities such as text, vision, and audio.",
  "documentation": {
    "entry": "transformers",
    "example": "from transformers import pipeline; print(pipeline('sentiment-analysis')('I love KFIOSA'))",
    "import": "transformers",
    "pypi": "https://pypi.org/project/transformers/"
  },
  "full_name": "transformers",
  "id": "pypi:transformers",
  "kind": "python_library",
  "metadata": {
    "category": "ai",
    "entry": "transformers",
    "example": "from transformers import pipeline; print(pipeline('sentiment-analysis')('I love KFIOSA'))",
    "import_name": "transformers",
    "pip_name": "transformers",
    "version": "4.44.0"
  },
  "metadata_status": "indexed",
  "name": "transformers",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "State-of-the-art ML for PyTorch / TensorFlow / JAX (LLMs, vision, ...).",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/transformers/",
  "tags": [
    "offensive",
    "transformers",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/transformers && python3 transformers.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/transformers && python3 transformers.py --help",
    "cd toolboxes/transformers && python3 transformers.py --help",
    "cd toolboxes/transformers && python3 transformers.py --help",
    "cd toolboxes/transformers && python3 transformers.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST transformers --output $KFIOSA_OUTPUT_DIR/transformers.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST transformers --output $KFIOSA_OUTPUT_DIR/transformers.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}