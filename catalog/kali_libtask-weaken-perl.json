{
  "id": "kali:libtask-weaken-perl",
  "kind": "kali_source_package",
  "name": "libtask-weaken-perl",
  "title": "libtask-weaken-perl",
  "summary": "module for ensuring that a platform has weaken support",
  "description": "module for ensuring that a platform has weaken support\nOne recurring problem in modules that use Scalar::Util's weaken function is\nthat it is not present in the pure-perl variant.\n.\nWhile this isn't necessarily always a problem in a straight CPAN-based Perl\nenvironment, some operating system distributions only include the pure-Perl\nversions, don't include the XS version, and so weaken is then \"missing\" from\nthe platform, despite passing a dependency on Scalar::Util successfully.\n.\nThe normal solution is to manually write tests in each distribution to ensure\nthat weaken is available.\n.\nTask::Weaken restores the functionality testing to a dependency you do once\nin your Makefile.PL, rather than something you have to write extra tests for\neach time you write a module.",
  "version": "1.06-2",
  "architectures": [
    "all"
  ],
  "homepage": "https://metacpan.org/release/Task-Weaken",
  "repository": "https://metacpan.org/release/Task-Weaken",
  "source_package": "libtask-weaken-perl",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libtask-weaken-perl"
  },
  "packages": [
    {
      "name": "libtask-weaken-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/Task::Weaken.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libtask-weaken-perl because module for ensuring that a platform has weaken support.",
    "When the engagement is in the install phase, use libtask-weaken-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libtask-weaken-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libtask-weaken-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "module for ensuring that a platform has weaken support\nOne recurring problem in modules that use Scalar::Util's weaken function is\nthat it is not present in the pure-perl variant.\n.\nWhile this isn't necessarily always a problem in a straight CPAN-based Perl\nenvironment, some operating system distributions only include the pure-Perl\nversions, don't include the XS version, and so weaken is then \"missing\" from\nthe platform, despite passing a dependency on Scalar::Util successfully.\n.\nThe normal solution is to manually write tests in each distribution to ensure\nthat weaken is available.\n.\nTask::Weaken restores the functionality testing to a dependency you do once\nin your Makefile.PL, rather than something you have to write extra tests for\neach time you write a module.",
    "how_to_use": "Install/setup: sudo apt install libtask-weaken-perl. module for ensuring that a platform has weaken support.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: module for ensuring that a platform has weaken support.",
    "why_to_use": "Prefer this tool based on upstream description: module for ensuring that a platform has weaken support\nOne recurring problem in modules that use Scalar::Util's weaken function is\nthat it is not present in the pure-perl variant.\n.\nWhile this isn't necessarily always a problem in a straight CPAN-based Perl\nenvironment, some operating system distributions only include the pure-Perl\nversions, don't include the XS version, and so weaken is then \"mis.",
    "usage_sections": [
      "Install with: sudo apt install libtask-weaken-perl",
      "Install with: sudo apt install libtask-weaken-perl"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
