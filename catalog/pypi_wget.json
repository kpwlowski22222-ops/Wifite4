{
  "category": "utility",
  "description": "wget is a pure Python download utility for Python 2 and Python 3.",
  "documentation": {
    "entry": "wget",
    "example": "import wget; wget.download('https://example.com/file')",
    "import": "wget",
    "pypi": "https://pypi.org/project/wget/"
  },
  "full_name": "wget",
  "id": "pypi:wget",
  "kind": "python_library",
  "metadata": {
    "category": "utility",
    "entry": "wget",
    "example": "import wget; wget.download('https://example.com/file')",
    "import_name": "wget",
    "pip_name": "wget",
    "version": "3.2"
  },
  "metadata_status": "indexed",
  "name": "wget",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "Pure-Python wget (download files).",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/wget/",
  "tags": [
    "offensive",
    "wget",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/wget && python3 wget.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/wget && python3 wget.py --help",
    "cd toolboxes/wget && python3 wget.py --help",
    "cd toolboxes/wget && python3 wget.py --help",
    "cd toolboxes/wget && python3 wget.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST wget --output $KFIOSA_OUTPUT_DIR/wget.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST wget --output $KFIOSA_OUTPUT_DIR/wget.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}