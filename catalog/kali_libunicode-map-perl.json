{
  "id": "kali:libunicode-map-perl",
  "kind": "kali_source_package",
  "name": "libunicode-map-perl",
  "title": "libunicode-map-perl",
  "summary": "Perl module for mapping charsets from and to UTF16 Unicode",
  "description": "Perl module for mapping charsets from and to UTF16 Unicode\nThis module converts strings from and to 2-byte Unicode UCS2 format.\nAll mappings happen via 2 byte UTF16 encodings, not via 1 byte UTF8\nencoding.  To convert between UTF8 and UTF16 use Unicode::String.\n.\nFor historical reasons this module coexists with Unicode::Map8.\nPlease use Unicode::Map8 unless you need to care for >1 byte\ncharacter sets, e.g. chinese GB2312.  Anyway, if you stick to the\nbasic functionality (see documentation) you can use both modules\nequivalently.\n.\nThe 'libwww-perl' package is needed to run the example mirrorMappings.",
  "version": "0.112-13+b5",
  "architectures": [
    "amd64"
  ],
  "homepage": "https://metacpan.org/release/Unicode-Map",
  "repository": "https://metacpan.org/release/Unicode-Map",
  "source_package": "libunicode-map-perl (0.112-13)",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libunicode-map-perl"
  },
  "packages": [
    {
      "name": "libunicode-map-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/Unicode::Map.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libunicode-map-perl because Perl module for mapping charsets from and to UTF16 Unicode.",
    "When the engagement is in the install phase, use libunicode-map-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libunicode-map-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libunicode-map-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Perl module for mapping charsets from and to UTF16 Unicode\nThis module converts strings from and to 2-byte Unicode UCS2 format.\nAll mappings happen via 2 byte UTF16 encodings, not via 1 byte UTF8\nencoding.  To convert between UTF8 and UTF16 use Unicode::String.\n.\nFor historical reasons this module coexists with Unicode::Map8.\nPlease use Unicode::Map8 unless you need to care for >1 byte\ncharacter sets, e.g. chinese GB2312.  Anyway, if you stick to the\nbasic functionality (see documentation) you can use both modules\nequivalently.\n.\nThe 'libwww-perl' package is needed to run the example mirrorMappings.",
    "how_to_use": "Install/setup: sudo apt install libunicode-map-perl (0.112-13). Perl module for mapping charsets from and to UTF16 Unicode.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Perl module for mapping charsets from and to UTF16 Unicode.",
    "why_to_use": "Prefer this tool based on upstream description: Perl module for mapping charsets from and to UTF16 Unicode\nThis module converts strings from and to 2-byte Unicode UCS2 format.\nAll mappings happen via 2 byte UTF16 encodings, not via 1 byte UTF8\nencoding.  To convert between UTF8 and UTF16 use Unicode::String.\n.\nFor historical reasons this module coexists with Unicode::Map8.\nPlease use Unicode::Map8 unless you need to care for >1 byte\ncharacter s.",
    "usage_sections": [
      "Install with: sudo apt install libunicode-map-perl (0.112-13)",
      "Install with: sudo apt install libunicode-map-perl (0.112-13)"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
