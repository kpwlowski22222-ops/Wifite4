{
  "id": "kali:libnet-netmask-perl",
  "kind": "kali_source_package",
  "name": "libnet-netmask-perl",
  "title": "libnet-netmask-perl",
  "summary": "module to parse, manipulate and lookup IP network blocks",
  "description": "module to parse, manipulate and lookup IP network blocks\nNet::Netmask parses and understands IPv4 and IPv6 CIDR blocks.\nIt's built with an object-oriented interface,\nwith functions being methods that operate on a Net::Netmask object.\n.\nThese methods provide\nnearly all types of information about a network block\nthat you might want.\n.\nThere are also functions to insert a network block into a table\nand then later lookup network blocks by IP address using that table.\nThere are functions to turn a IP address range\ninto a list of CIDR blocks.\nThere are functions to turn a list of CIDR blocks\ninto a list of IP addresses.\n.\nThere is a function for sorting by text IP address.\n.\nAll functions understand both IPv4 and IPv6.\nMatches, finds, etc, will always return false\nwhen an IPv4 address is matched against an IPv6 address.",
  "version": "2.0003-1",
  "architectures": [
    "all"
  ],
  "homepage": "https://metacpan.org/release/Net-Netmask",
  "repository": "https://metacpan.org/release/Net-Netmask",
  "source_package": "libnet-netmask-perl",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libnet-netmask-perl"
  },
  "packages": [
    {
      "name": "libnet-netmask-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/Net::Netmask.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libnet-netmask-perl because module to parse, manipulate and lookup IP network blocks.",
    "When the engagement is in the install phase, use libnet-netmask-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libnet-netmask-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libnet-netmask-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "module to parse, manipulate and lookup IP network blocks\nNet::Netmask parses and understands IPv4 and IPv6 CIDR blocks.\nIt's built with an object-oriented interface,\nwith functions being methods that operate on a Net::Netmask object.\n.\nThese methods provide\nnearly all types of information about a network block\nthat you might want.\n.\nThere are also functions to insert a network block into a table\nand then later lookup network blocks by IP address using that table.\nThere are functions to turn a IP address range\ninto a list of CIDR blocks.\nThere are functions to turn a list of CIDR blocks\ninto a list of IP addresses.\n.\nThere is a function for sorting by text IP address.\n.\nAll functions understand both IPv4 and IPv6.\nMatches, finds, etc, will always return false\nwhen an IPv4 address is matched against an IPv6 address.",
    "how_to_use": "Install/setup: sudo apt install libnet-netmask-perl. module to parse, manipulate and lookup IP network blocks.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: module to parse, manipulate and lookup IP network blocks.",
    "why_to_use": "Prefer this tool based on upstream description: module to parse, manipulate and lookup IP network blocks\nNet::Netmask parses and understands IPv4 and IPv6 CIDR blocks.\nIt's built with an object-oriented interface,\nwith functions being methods that operate on a Net::Netmask object.\n.\nThese methods provide\nnearly all types of information about a network block\nthat you might want.\n.\nThere are also functions to insert a network block into a table\na.",
    "usage_sections": [
      "Install with: sudo apt install libnet-netmask-perl",
      "Install with: sudo apt install libnet-netmask-perl"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
