{
  "id": "kali:libmatch-simple-xs-perl",
  "kind": "kali_source_package",
  "name": "libmatch-simple-xs-perl",
  "title": "libmatch-simple-xs-perl",
  "summary": "XS backend for match::simple",
  "description": "XS backend for match::simple\nmatch::simple::XS is a faster XS-based implementation of match::simple.\n.\nDepending on what sort of matches done, it is likely to be several\ntimes faster. In extreme cases, such as matching a string in an\narrayref, it can be twenty-five times faster, or more. However, where\n$that is a single regexp, it's around 30% slower.\n.\nOverall though, the performance improvement should be worthwhile.",
  "version": "0.002-2",
  "architectures": [
    "amd64"
  ],
  "homepage": "https://metacpan.org/release/match-simple-XS",
  "repository": "https://metacpan.org/release/match-simple-XS",
  "source_package": "libmatch-simple-xs-perl",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libmatch-simple-xs-perl"
  },
  "packages": [
    {
      "name": "libmatch-simple-xs-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/match::simple::XS.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libmatch-simple-xs-perl because XS backend for match::simple.",
    "When the engagement is in the install phase, use libmatch-simple-xs-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libmatch-simple-xs-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libmatch-simple-xs-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "XS backend for match::simple\nmatch::simple::XS is a faster XS-based implementation of match::simple.\n.\nDepending on what sort of matches done, it is likely to be several\ntimes faster. In extreme cases, such as matching a string in an\narrayref, it can be twenty-five times faster, or more. However, where\n$that is a single regexp, it's around 30% slower.\n.\nOverall though, the performance improvement should be worthwhile.",
    "how_to_use": "Install/setup: sudo apt install libmatch-simple-xs-perl.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: XS backend for match::simple.",
    "why_to_use": "Prefer this tool based on upstream description: XS backend for match::simple\nmatch::simple::XS is a faster XS-based implementation of match::simple.\n.\nDepending on what sort of matches done, it is likely to be several\ntimes faster. In extreme cases, such as matching a string in an\narrayref, it can be twenty-five times faster, or more. However, where\n$that is a single regexp, it's around 30% slower.\n.\nOverall though, the performance improvement .",
    "usage_sections": [
      "Install with: sudo apt install libmatch-simple-xs-perl",
      "Install with: sudo apt install libmatch-simple-xs-perl"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
