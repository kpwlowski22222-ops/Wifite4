{
  "category": "utility",
  "description": "PyYAML is a YAML parser and emitter for Python. Used for parsing tool outputs (e.g., nmap, bloodhound).",
  "documentation": {
    "entry": "yaml",
    "example": "import yaml; print(yaml.safe_load('a: 1'))",
    "import": "yaml",
    "pypi": "https://pypi.org/project/pyyaml/"
  },
  "full_name": "yaml",
  "id": "pypi:pyyaml",
  "kind": "python_library",
  "metadata": {
    "category": "utility",
    "entry": "yaml",
    "example": "import yaml; print(yaml.safe_load('a: 1'))",
    "import_name": "yaml",
    "pip_name": "pyyaml",
    "version": "6.0.1"
  },
  "metadata_status": "indexed",
  "name": "pyyaml",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "YAML parser and emitter.",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/pyyaml/",
  "tags": [
    "offensive",
    "pyyaml",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/pyyaml && python3 pyyaml.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/pyyaml && python3 pyyaml.py --help",
    "cd toolboxes/pyyaml && python3 pyyaml.py --help",
    "cd toolboxes/pyyaml && python3 pyyaml.py --help",
    "cd toolboxes/pyyaml && python3 pyyaml.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST pyyaml --output $KFIOSA_OUTPUT_DIR/pyyaml.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST pyyaml --output $KFIOSA_OUTPUT_DIR/pyyaml.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}