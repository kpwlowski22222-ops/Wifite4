{
  "category": "utility",
  "description": "lzx is a Python wrapper for the LZX compression algorithm used in Windows Cabinet / WIM files.",
  "documentation": {
    "entry": "lzx",
    "example": "import lzx; print(lzx.decompress(b''))",
    "import": "lzx",
    "pypi": "https://pypi.org/project/lzx/"
  },
  "full_name": "lzx",
  "id": "pypi:lzx",
  "kind": "python_library",
  "metadata": {
    "category": "utility",
    "entry": "lzx",
    "example": "import lzx; print(lzx.decompress(b''))",
    "import_name": "lzx",
    "pip_name": "lzx",
    "version": "0.0.1"
  },
  "metadata_status": "indexed",
  "name": "lzx",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "LZX compression (Windows Cabinet / WIM).",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/lzx/",
  "tags": [
    "offensive",
    "lzx",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/lzx && python3 lzx.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/lzx && python3 lzx.py --help",
    "cd toolboxes/lzx && python3 lzx.py --help",
    "cd toolboxes/lzx && python3 lzx.py --help",
    "cd toolboxes/lzx && python3 lzx.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST lzx --output $KFIOSA_OUTPUT_DIR/lzx.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST lzx --output $KFIOSA_OUTPUT_DIR/lzx.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}