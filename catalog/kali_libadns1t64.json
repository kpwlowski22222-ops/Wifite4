{
  "id": "kali:libadns1t64",
  "kind": "kali_source_package",
  "name": "libadns1t64",
  "title": "libadns1t64",
  "summary": "Asynchronous-capable DNS client library",
  "description": "Asynchronous-capable DNS client library\nadns is a resolver library for C (and C++) programs.  In contrast\nwith the existing interfaces, gethostbyname et al and libresolv, it\ncan be used in an asynchronous, non-blocking manner, and has a\nconvenient interface which doesn't require the application to handle\nany DNS data formats.  Many queries can be handled simultaneously.\n.\nThis package contains the runtime library.",
  "version": "1.6.2-3",
  "architectures": [
    "amd64"
  ],
  "homepage": "http://www.chiark.greenend.org.uk/~ian/adns/",
  "repository": "http://www.chiark.greenend.org.uk/~ian/adns/",
  "source_package": "adns",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libadns1t64"
  },
  "packages": [
    {
      "name": "libadns1t64"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libadns1t64 because Asynchronous-capable DNS client library.",
    "When the engagement is in the install phase, use libadns1t64 as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libadns1t64 with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libadns1t64 --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Asynchronous-capable DNS client library\nadns is a resolver library for C (and C++) programs.  In contrast\nwith the existing interfaces, gethostbyname et al and libresolv, it\ncan be used in an asynchronous, non-blocking manner, and has a\nconvenient interface which doesn't require the application to handle\nany DNS data formats.  Many queries can be handled simultaneously.\n.\nThis package contains the runtime library.",
    "how_to_use": "Install/setup: sudo apt install adns.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Asynchronous-capable DNS client library.",
    "why_to_use": "Prefer this tool based on upstream description: Asynchronous-capable DNS client library\nadns is a resolver library for C (and C++) programs.  In contrast\nwith the existing interfaces, gethostbyname et al and libresolv, it\ncan be used in an asynchronous, non-blocking manner, and has a\nconvenient interface which doesn't require the application to handle\nany DNS data formats.  Many queries can be handled simultaneously.\n.\nThis package contains the.",
    "usage_sections": [
      "Install with: sudo apt install adns",
      "Install with: sudo apt install adns"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
