{
  "category": "web",
  "description": "Requests is an elegant and simple HTTP library for Python, built for human beings. The de-facto standard for HTTP clients in Python.",
  "documentation": {
    "entry": "requests",
    "example": "import requests; r = requests.get('https://example.com'); print(r.status_code)",
    "import": "requests",
    "pypi": "https://pypi.org/project/requests/"
  },
  "full_name": "requests",
  "id": "pypi:requests",
  "kind": "python_library",
  "metadata": {
    "category": "web",
    "entry": "requests",
    "example": "import requests; r = requests.get('https://example.com'); print(r.status_code)",
    "import_name": "requests",
    "pip_name": "requests",
    "version": "2.32.0"
  },
  "metadata_status": "indexed",
  "name": "requests",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "Elegant HTTP for Humans.",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/requests/",
  "tags": [
    "offensive",
    "requests",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/requests && python3 requests.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/requests && python3 requests.py --help",
    "cd toolboxes/requests && python3 requests.py --help",
    "cd toolboxes/requests && python3 requests.py --help",
    "cd toolboxes/requests && python3 requests.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST requests --output $KFIOSA_OUTPUT_DIR/requests.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST requests --output $KFIOSA_OUTPUT_DIR/requests.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}