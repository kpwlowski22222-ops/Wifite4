{
  "id": "kali:libxml-parser-perl",
  "kind": "kali_source_package",
  "name": "libxml-parser-perl",
  "title": "libxml-parser-perl",
  "summary": "Perl module for parsing XML files",
  "description": "Perl module for parsing XML files\nThe XML::Parser module provides ways to parse XML documents.  It is built on\ntop of XML::Parser::Expat, which is a lower level interface to James Clark's\nexpat library. Each call to one of the parsing methods creates a new instance\nof XML::Parser::Expat which is then used to parse the document. Expat options\nmay be provided when the XML::Parser object is created. These options are\nthen passed on to the Expat object on each parse call. They can also be given\nas extra arguments to the parse methods, in which case they override options\ngiven at XML::Parser creation time.\n.\nThe behavior of the parser is controlled either by Style and/or Handlers\noptions, or by setHandlers method. These all provide mechanisms for\nXML::Parser to set the handlers needed by XML::Parser::Expat. If neither\nStyle nor Handlers are specified, then parsing just checks the document for\nbeing well-formed.\n.\nWhen underlying handlers get called, they receive as their first parameter\nthe Expat object, not the Parser object.",
  "version": "2.47-2",
  "architectures": [
    "amd64"
  ],
  "homepage": "https://metacpan.org/release/XML-Parser",
  "repository": "https://metacpan.org/release/XML-Parser",
  "source_package": "libxml-parser-perl",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libxml-parser-perl"
  },
  "packages": [
    {
      "name": "libxml-parser-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/XML::Parser.3pm.gz",
    "/usr/share/man/man3/XML::Parser::Expat.3pm.gz",
    "/usr/share/man/man3/XML::Parser::Style::Debug.3pm.gz",
    "/usr/share/man/man3/XML::Parser::Style::Objects.3pm.gz",
    "/usr/share/man/man3/XML::Parser::Style::Stream.3pm.gz",
    "/usr/share/man/man3/XML::Parser::Style::Subs.3pm.gz",
    "/usr/share/man/man3/XML::Parser::Style::Tree.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libxml-parser-perl because Perl module for parsing XML files.",
    "When the engagement is in the install phase, use libxml-parser-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libxml-parser-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libxml-parser-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Perl module for parsing XML files\nThe XML::Parser module provides ways to parse XML documents.  It is built on\ntop of XML::Parser::Expat, which is a lower level interface to James Clark's\nexpat library. Each call to one of the parsing methods creates a new instance\nof XML::Parser::Expat which is then used to parse the document. Expat options\nmay be provided when the XML::Parser object is created. These options are\nthen passed on to the Expat object on each parse call. They can also be given\nas extra arguments to the parse methods, in which case they override options\ngiven at XML::Parser creation time.\n.\nThe behavior of the parser is controlled either by Style and/or Handlers\noptions, or by setHandlers method. These all provide mechanisms for\nXML::Parser to set the handlers needed by XML::Parser::Expat. If neither\nStyle nor Handlers are specified, then parsing just checks the document for\nbeing well-formed.\n.\nWhen underlying handlers get called, they receive as their first parameter\nthe Expat object, not the Parser object.",
    "how_to_use": "Install/setup: sudo apt install libxml-parser-perl.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Perl module for parsing XML files.",
    "why_to_use": "Prefer this tool based on upstream description: Perl module for parsing XML files\nThe XML::Parser module provides ways to parse XML documents.  It is built on\ntop of XML::Parser::Expat, which is a lower level interface to James Clark's\nexpat library. Each call to one of the parsing methods creates a new instance\nof XML::Parser::Expat which is then used to parse the document. Expat options\nmay be provided when the XML::Parser object is created. .",
    "usage_sections": [
      "Install with: sudo apt install libxml-parser-perl",
      "Install with: sudo apt install libxml-parser-perl"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
