{
  "id": "kali:libtie-ixhash-perl",
  "kind": "kali_source_package",
  "name": "libtie-ixhash-perl",
  "title": "libtie-ixhash-perl",
  "summary": "Perl module to order associative arrays",
  "description": "Perl module to order associative arrays\nTie::IxHash implements Perl hashes that preserve the order in which the\nhash elements were added. The order is not affected when values\ncorresponding to existing keys in the IxHash are changed. The elements can\nalso be set to any arbitrary supplied order. The familiar perl array\noperations can also be performed on the IxHash.",
  "version": "1.23-4",
  "architectures": [
    "all"
  ],
  "homepage": "https://metacpan.org/release/Tie-IxHash",
  "repository": "https://metacpan.org/release/Tie-IxHash",
  "source_package": "libtie-ixhash-perl",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libtie-ixhash-perl"
  },
  "packages": [
    {
      "name": "libtie-ixhash-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/Tie::IxHash.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libtie-ixhash-perl because Perl module to order associative arrays.",
    "When the engagement is in the install phase, use libtie-ixhash-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libtie-ixhash-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libtie-ixhash-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Perl module to order associative arrays\nTie::IxHash implements Perl hashes that preserve the order in which the\nhash elements were added. The order is not affected when values\ncorresponding to existing keys in the IxHash are changed. The elements can\nalso be set to any arbitrary supplied order. The familiar perl array\noperations can also be performed on the IxHash.",
    "how_to_use": "Install/setup: sudo apt install libtie-ixhash-perl.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Perl module to order associative arrays.",
    "why_to_use": "Prefer this tool based on upstream description: Perl module to order associative arrays\nTie::IxHash implements Perl hashes that preserve the order in which the\nhash elements were added. The order is not affected when values\ncorresponding to existing keys in the IxHash are changed. The elements can\nalso be set to any arbitrary supplied order. The familiar perl array\noperations can also be performed on the IxHash.",
    "usage_sections": [
      "Install with: sudo apt install libtie-ixhash-perl",
      "Install with: sudo apt install libtie-ixhash-perl"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
