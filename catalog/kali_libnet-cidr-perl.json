{
  "id": "kali:libnet-cidr-perl",
  "kind": "kali_source_package",
  "name": "libnet-cidr-perl",
  "title": "libnet-cidr-perl",
  "summary": "module to manipulate IPv4/IPv6 netblocks in CIDR notation",
  "description": "module to manipulate IPv4/IPv6 netblocks in CIDR notation\nThe Net::CIDR module contains functions that manipulate lists of IP netblocks\nexpressed in Classless Inter-Domain Routing (CIDR) notation.\n.\nNet::CIDR handles both IPv4 and IPv6 addresses.",
  "version": "0.27-1",
  "architectures": [
    "all"
  ],
  "homepage": "https://metacpan.org/release/Net-CIDR",
  "repository": "https://metacpan.org/release/Net-CIDR",
  "source_package": "libnet-cidr-perl",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libnet-cidr-perl"
  },
  "packages": [
    {
      "name": "libnet-cidr-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/Net::CIDR.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libnet-cidr-perl because module to manipulate IPv4/IPv6 netblocks in CIDR notation.",
    "When the engagement is in the install phase, use libnet-cidr-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libnet-cidr-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libnet-cidr-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "module to manipulate IPv4/IPv6 netblocks in CIDR notation\nThe Net::CIDR module contains functions that manipulate lists of IP netblocks\nexpressed in Classless Inter-Domain Routing (CIDR) notation.\n.\nNet::CIDR handles both IPv4 and IPv6 addresses.",
    "how_to_use": "Install/setup: sudo apt install libnet-cidr-perl. module to manipulate IPv4/IPv6 netblocks in CIDR notation.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: module to manipulate IPv4/IPv6 netblocks in CIDR notation.",
    "why_to_use": "Prefer this tool based on upstream description: module to manipulate IPv4/IPv6 netblocks in CIDR notation\nThe Net::CIDR module contains functions that manipulate lists of IP netblocks\nexpressed in Classless Inter-Domain Routing (CIDR) notation.\n.\nNet::CIDR handles both IPv4 and IPv6 addresses.",
    "usage_sections": [
      "Install with: sudo apt install libnet-cidr-perl",
      "Install with: sudo apt install libnet-cidr-perl"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
