{
  "category": "osint",
  "description": "phonenumbers is a Python port of Google's libphonenumber library. Validates, formats, and looks up phone numbers.",
  "documentation": {
    "entry": "phonenumbers",
    "example": "import phonenumbers; print(phonenumbers.parse('+12025551234'))",
    "import": "phonenumbers",
    "pypi": "https://pypi.org/project/phonenumbers/"
  },
  "full_name": "phonenumbers",
  "id": "pypi:phonenumbers",
  "kind": "python_library",
  "metadata": {
    "category": "osint",
    "entry": "phonenumbers",
    "example": "import phonenumbers; print(phonenumbers.parse('+12025551234'))",
    "import_name": "phonenumbers",
    "pip_name": "phonenumbers",
    "version": "8.13.40"
  },
  "metadata_status": "indexed",
  "name": "phonenumbers",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "Phone number parsing / formatting / carrier lookup.",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/phonenumbers/",
  "tags": [
    "offensive",
    "phonenumbers",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/phonenumbers && python3 phonenumbers.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/phonenumbers && python3 phonenumbers.py --help",
    "cd toolboxes/phonenumbers && python3 phonenumbers.py --help",
    "cd toolboxes/phonenumbers && python3 phonenumbers.py --help",
    "cd toolboxes/phonenumbers && python3 phonenumbers.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST phonenumbers --output $KFIOSA_OUTPUT_DIR/phonenumbers.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST phonenumbers --output $KFIOSA_OUTPUT_DIR/phonenumbers.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}