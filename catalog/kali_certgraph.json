{
  "id": "kali:certgraph",
  "kind": "kali_source_package",
  "name": "certgraph",
  "title": "certgraph",
  "summary": "tool to crawl the graph of certificate Alternate Names",
  "description": "tool to crawl the graph of certificate Alternate Names\nThis package contains a tool to crawl the graph of certificate Alternate\nNames. CertGraph crawls SSL certificates creating a directed graph where each\ndomain is a node and the certificate alternative names for that domain's\ncertificate are the edges to other domain nodes. New domains are printed as\nthey are found. In Detailed mode upon completion the Graph's adjacency list is\nprinted.\n.\nCrawling defaults to collecting certificate by connecting over TCP, however\nthere are multiple drivers that can search Certificate Transparency logs.\n.\nThis tool was designed to be used for host name enumeration via SSL\ncertificates, but it can also show you a \"chain\" of trust between domains and\nthe certificates that re-used between them.",
  "version": "20180911-0kali1",
  "architectures": [
    "amd64"
  ],
  "homepage": "https://github.com/lanrat/certgraph",
  "repository": "https://github.com/lanrat/certgraph",
  "source_package": "certgraph",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install certgraph"
  },
  "packages": [
    {
      "name": "certgraph"
    }
  ],
  "commands": [
    {
      "name": "certgraph",
      "path": "/usr/bin/certgraph",
      "description": "Binary from certgraph",
      "usage": "certgraph [options] [args]",
      "arguments": []
    }
  ],
  "files": [
    "/usr/bin/certgraph"
  ],
  "tags": [
    "os-package"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "command_examples": [
    "certgraph [options] [args]"
  ],
  "use_cases": [
    "When you need Kali Linux, use certgraph because tool to crawl the graph of certificate Alternate Names.",
    "When the engagement is in the install phase, use certgraph as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When you need a concrete invocation, run `certgraph [options] [args]` as documented upstream for certgraph.",
    "When chaining Kali Linux tools, pair certgraph with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `certgraph --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "examples": [
      "certgraph [options] [args]"
    ],
    "readme": "tool to crawl the graph of certificate Alternate Names\nThis package contains a tool to crawl the graph of certificate Alternate\nNames. CertGraph crawls SSL certificates creating a directed graph where each\ndomain is a node and the certificate alternative names for that domain's\ncertificate are the edges to other domain nodes. New domains are printed as\nthey are found. In Detailed mode upon completion the Graph's adjacency list is\nprinted.\n.\nCrawling defaults to collecting certificate by connecting over TCP, however\nthere are multiple drivers that can search Certificate Transparency logs.\n.\nThis tool was designed to be used for host name enumeration via SSL\ncertificates, but it can also show you a \"chain\" of trust between domains and\nthe certificates that re-used between them.",
    "how_to_use": "Install/setup: sudo apt install certgraph. tool to crawl the graph of certificate Alternate Names.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: tool to crawl the graph of certificate Alternate Names.",
    "why_to_use": "Prefer this tool based on upstream description: tool to crawl the graph of certificate Alternate Names\nThis package contains a tool to crawl the graph of certificate Alternate\nNames. CertGraph crawls SSL certificates creating a directed graph where each\ndomain is a node and the certificate alternative names for that domain's\ncertificate are the edges to other domain nodes. New domains are printed as\nthey are found. In Detailed mode upon complet.",
    "usage_sections": [
      "Install with: sudo apt install certgraph",
      "Install with: sudo apt install certgraph"
    ]
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline"
}
