{
  "id": "kali:bloodyad",
  "kind": "kali_source_package",
  "name": "bloodyad",
  "title": "bloodyad",
  "summary": "Active Directory privilege escalation framework",
  "description": "Active Directory privilege escalation framework\nbloodyAD can perform specific LDAP calls to a domain controller in order to\nperform AD privesc. It supports authentication using cleartext passwords,\npass-the-hash, pass-the-ticket or certificates and binds to LDAP services of a\ndomain controller to perform AD privesc.\n.\nExchange of sensitive information without LDAPS is supported. It is also\ndesigned to be used transparently with a SOCKS proxy.",
  "version": "2.5.4-0kali5",
  "architectures": [
    "amd64"
  ],
  "homepage": "https://github.com/CravateRouge/bloodyAD",
  "repository": "https://github.com/CravateRouge/bloodyAD",
  "source_package": "bloodyad",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install bloodyad"
  },
  "packages": [
    {
      "name": "bloodyad"
    }
  ],
  "commands": [
    {
      "name": "bloodyad",
      "path": "/usr/bin/bloodyad",
      "description": "usage: bloodyad [-h] [-d DOMAIN] [-u USERNAME] [-p PASSWORD]",
      "usage": "bloodyad [options] [args]",
      "arguments": []
    }
  ],
  "files": [
    "/usr/bin/bloodyad"
  ],
  "tags": [
    "os-package"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "command_examples": [
    "bloodyad [options] [args]"
  ],
  "use_cases": [
    "When you need Kali Linux, use bloodyad because Active Directory privilege escalation framework.",
    "When the engagement is in the install phase, use bloodyad as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When you need a concrete invocation, run `bloodyad [options] [args]` as documented upstream for bloodyad.",
    "When chaining Kali Linux tools, pair bloodyad with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `bloodyad --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "examples": [
      "bloodyad [options] [args]"
    ],
    "readme": "Active Directory privilege escalation framework\nbloodyAD can perform specific LDAP calls to a domain controller in order to\nperform AD privesc. It supports authentication using cleartext passwords,\npass-the-hash, pass-the-ticket or certificates and binds to LDAP services of a\ndomain controller to perform AD privesc.\n.\nExchange of sensitive information without LDAPS is supported. It is also\ndesigned to be used transparently with a SOCKS proxy.",
    "how_to_use": "Install/setup: sudo apt install bloodyad. Active Directory privilege escalation framework.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Active Directory privilege escalation framework.",
    "why_to_use": "Prefer this tool based on upstream description: Active Directory privilege escalation framework\nbloodyAD can perform specific LDAP calls to a domain controller in order to\nperform AD privesc. It supports authentication using cleartext passwords,\npass-the-hash, pass-the-ticket or certificates and binds to LDAP services of a\ndomain controller to perform AD privesc.\n.\nExchange of sensitive information without LDAPS is supported. It is also\ndesigne.",
    "usage_sections": [
      "Install with: sudo apt install bloodyad",
      "Install with: sudo apt install bloodyad"
    ]
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline"
}
