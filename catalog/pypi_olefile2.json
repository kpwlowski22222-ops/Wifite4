{
  "category": "exploit",
  "description": "Legacy fork of olefile for reading OLE2 / compound documents.",
  "documentation": {
    "entry": "olefile",
    "example": "import olefile; print(olefile.OleFileIO('doc.doc').exists('worddocument'))",
    "import": "olefile",
    "pypi": "https://pypi.org/project/olefile2/"
  },
  "full_name": "olefile",
  "id": "pypi:olefile2",
  "kind": "python_library",
  "metadata": {
    "category": "exploit",
    "entry": "olefile",
    "example": "import olefile; print(olefile.OleFileIO('doc.doc').exists('worddocument'))",
    "import_name": "olefile",
    "pip_name": "olefile2",
    "version": "0.0.1"
  },
  "metadata_status": "indexed",
  "name": "olefile2",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "OLE2 parser (legacy fork).",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/olefile2/",
  "tags": [
    "offensive",
    "olefile2",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/olefile2 && python3 olefile2.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/olefile2 && python3 olefile2.py --help",
    "cd toolboxes/olefile2 && python3 olefile2.py --help",
    "cd toolboxes/olefile2 && python3 olefile2.py --help",
    "cd toolboxes/olefile2 && python3 olefile2.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST olefile2 --output $KFIOSA_OUTPUT_DIR/olefile2.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST olefile2 --output $KFIOSA_OUTPUT_DIR/olefile2.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}