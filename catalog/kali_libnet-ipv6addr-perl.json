{
  "id": "kali:libnet-ipv6addr-perl",
  "kind": "kali_source_package",
  "name": "libnet-ipv6addr-perl",
  "title": "libnet-ipv6addr-perl",
  "summary": "module to validate/manipulate IPv6 addresses",
  "description": "module to validate/manipulate IPv6 addresses\nNet::IPv6Addr checks if strings are valid IPv6 addresses, as specified in\nRFC1884, and can convert them into other supported formats including:\n.\n* Preferred form x:x:x:x:x:x:x:x\n* Compressed form with double colon: x::x\n* Mixed IPv4/IPv6 format: x:x:x:x:x:x:d.d.d.d\n* Mixed IPv4/IPv6 with compression: x::x:d.d.d.d\n* Base-85-encoded\n* Big integers\n* Arrays\n* Reverse-address pointer\n.\nWhen creating/validating an IPv6 address from a given string, Net::IPv6Addr\nwill either accept it or throw an exception.",
  "version": "1.02-1",
  "architectures": [
    "all"
  ],
  "homepage": "https://metacpan.org/release/Net-IPv6Addr",
  "repository": "https://metacpan.org/release/Net-IPv6Addr",
  "source_package": "libnet-ipv6addr-perl",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libnet-ipv6addr-perl"
  },
  "packages": [
    {
      "name": "libnet-ipv6addr-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/Net::IPv6Addr.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libnet-ipv6addr-perl because module to validate/manipulate IPv6 addresses.",
    "When the engagement is in the install phase, use libnet-ipv6addr-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libnet-ipv6addr-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libnet-ipv6addr-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "module to validate/manipulate IPv6 addresses\nNet::IPv6Addr checks if strings are valid IPv6 addresses, as specified in\nRFC1884, and can convert them into other supported formats including:\n.\n* Preferred form x:x:x:x:x:x:x:x\n* Compressed form with double colon: x::x\n* Mixed IPv4/IPv6 format: x:x:x:x:x:x:d.d.d.d\n* Mixed IPv4/IPv6 with compression: x::x:d.d.d.d\n* Base-85-encoded\n* Big integers\n* Arrays\n* Reverse-address pointer\n.\nWhen creating/validating an IPv6 address from a given string, Net::IPv6Addr\nwill either accept it or throw an exception.",
    "how_to_use": "Install/setup: sudo apt install libnet-ipv6addr-perl. module to validate/manipulate IPv6 addresses.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: module to validate/manipulate IPv6 addresses.",
    "why_to_use": "Prefer this tool based on upstream description: module to validate/manipulate IPv6 addresses\nNet::IPv6Addr checks if strings are valid IPv6 addresses, as specified in\nRFC1884, and can convert them into other supported formats including:\n.\n* Preferred form x:x:x:x:x:x:x:x\n* Compressed form with double colon: x::x\n* Mixed IPv4/IPv6 format: x:x:x:x:x:x:d.d.d.d\n* Mixed IPv4/IPv6 with compression: x::x:d.d.d.d\n* Base-85-encoded\n* Big integers\n* Arra.",
    "usage_sections": [
      "Install with: sudo apt install libnet-ipv6addr-perl",
      "Install with: sudo apt install libnet-ipv6addr-perl"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
