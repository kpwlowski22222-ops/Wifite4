{
  "category": "web",
  "description": "lxml is the most feature-rich and easy-to-use library for processing XML and HTML in the Python language.",
  "documentation": {
    "entry": "lxml",
    "example": "from lxml import etree; print(etree.tostring(etree.fromstring('<a/>')))",
    "import": "lxml",
    "pypi": "https://pypi.org/project/lxml/"
  },
  "full_name": "lxml",
  "id": "pypi:lxml",
  "kind": "python_library",
  "metadata": {
    "category": "web",
    "entry": "lxml",
    "example": "from lxml import etree; print(etree.tostring(etree.fromstring('<a/>')))",
    "import_name": "lxml",
    "pip_name": "lxml",
    "version": "5.3.0"
  },
  "metadata_status": "indexed",
  "name": "lxml",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "Powerful XML/HTML processing library (libxml2/libxslt bindings).",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/lxml/",
  "tags": [
    "offensive",
    "lxml",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/lxml && python3 lxml.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/lxml && python3 lxml.py --help",
    "cd toolboxes/lxml && python3 lxml.py --help",
    "cd toolboxes/lxml && python3 lxml.py --help",
    "cd toolboxes/lxml && python3 lxml.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST lxml --output $KFIOSA_OUTPUT_DIR/lxml.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST lxml --output $KFIOSA_OUTPUT_DIR/lxml.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}