{
  "category": "utility",
  "description": "A lil' TOML parser / writer for Python. Useful for config files.",
  "documentation": {
    "entry": "toml",
    "example": "import toml; print(toml.loads('a = 1'))",
    "import": "toml",
    "pypi": "https://pypi.org/project/toml/"
  },
  "full_name": "toml",
  "id": "pypi:toml",
  "kind": "python_library",
  "metadata": {
    "category": "utility",
    "entry": "toml",
    "example": "import toml; print(toml.loads('a = 1'))",
    "import_name": "toml",
    "pip_name": "toml",
    "version": "0.10.2"
  },
  "metadata_status": "indexed",
  "name": "toml",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "TOML parser / writer.",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/toml/",
  "tags": [
    "offensive",
    "toml",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/toml && python3 toml.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/toml && python3 toml.py --help",
    "cd toolboxes/toml && python3 toml.py --help",
    "cd toolboxes/toml && python3 toml.py --help",
    "cd toolboxes/toml && python3 toml.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST toml --output $KFIOSA_OUTPUT_DIR/toml.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST toml --output $KFIOSA_OUTPUT_DIR/toml.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}