{
  "id": "kali:tftpd32",
  "kind": "kali_source_package",
  "name": "tftpd32",
  "title": "tftpd32",
  "summary": "### tftpd32",
  "version": "4.50-0kali3",
  "architectures": [
    "all"
  ],
  "homepage": "https://tftpd32.jounin.net/tftpd32.html",
  "repository": "https://gitlab.com/kalilinux/packages/tftpd32",
  "metapackages": [
    "kali-linux-everything",
    "kali-linux-large",
    "kali-tools-windows-resources"
  ],
  "install": {
    "apt": "sudo apt install tftpd32"
  },
  "packages": [
    {
      "name": "tftpd32"
    }
  ],
  "commands": [
    {
      "name": "tftpd32",
      "description": "- - -",
      "usage": null,
      "arguments": [],
      "raw_help": "",
      "examples": [],
      "risk": {
        "level": "low",
        "signals": [],
        "requires_explicit_authorization": false,
        "allow_autonomous_execution": true,
        "examples_policy": "operational"
      },
      "local_help": {
        "--help": "/usr/bin/tftpd32: 3: kali_winexe: not found",
        "-h": "/usr/bin/tftpd32: 3: kali_winexe: not found",
        "help": "/usr/bin/tftpd32: 3: kali_winexe: not found"
      },
      "binary_metadata": {
        "path": "/usr/bin/tftpd32",
        "size_bytes": 80,
        "is_symlink": false,
        "file_type": "POSIX shell script, ASCII text executable"
      }
    }
  ],
  "risk": {
    "level": "low",
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ],
    "requires_explicit_authorization": false,
    "allow_autonomous_execution": true,
    "examples_policy": "operational"
  },
  "source": {
    "official": true,
    "page": "https://www.kali.org/tools/tftpd32/",
    "raw_markdown": "https://gitlab.com/kalilinux/documentation/kali-tools/-/raw/main/tftpd32/index.md",
    "retrieved_at": "2026-07-14T13:08:41+00:00",
    "sha256": "fcfb9e955ec3d77e2a4f7939910bdb05a884f85d7b1d56cd4438b522f08dec4e"
  },
  "apt_metadata": {
    "section": "utils",
    "installed_size": "590",
    "maintainer": "Kali Developers <devel@kali.org>",
    "architecture": "all",
    "version": "4.50-0kali3",
    "depends": "kali-defaults",
    "description": "Open source ipv6-ready TFTP server for Windows\nTftpd32 is a free, opensource IPv6 ready application which includes DHCP, TFTP,\nDNS, SNTP and Syslog servers as well as a TFTP client.\nThe TFTP client and server are fully compatible with TFTP option support\n(tsize, blocksize and timeout), which allow the maximum performance when\ntransferring the data.\nSome extended features such as directory facility, security tuning, interface\nfiltering; progress bars and early acknowledgments enhance usefulness and\nthroughput of the TFTP protocol for both client and server.\nThe included DHCP server provides unlimited automatic or static IP address\nassignment.",
    "homepage": "https://tftpd32.jounin.net/tftpd32.html"
  },
  "tags": [
    "offensive",
    "tftpd32",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/tftpd32 && python3 tftpd32.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/tftpd32 && python3 tftpd32.py --help",
    "cd toolboxes/tftpd32 && python3 tftpd32.py --help",
    "cd toolboxes/tftpd32 && python3 tftpd32.py --help",
    "cd toolboxes/tftpd32 && python3 tftpd32.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST tftpd32 --output $KFIOSA_OUTPUT_DIR/tftpd32.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST tftpd32 --output $KFIOSA_OUTPUT_DIR/tftpd32.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}