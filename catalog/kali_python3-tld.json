{
  "id": "kali:python3-tld",
  "kind": "kali_source_package",
  "name": "python3-tld",
  "title": "python3-tld",
  "summary": "Extract the top level domain (TLD) from a given URL (Python 3)",
  "description": "Extract the top level domain (TLD) from a given URL (Python 3)\nThis package contains a module which extracts the top level domain (TLD) from\nthe given URL. The list of TLD names is taken from Mozilla.\n.\nIt can optionally raises exceptions on non-existing TLDs or silently fails (if\nfail_silently argument is set to True).",
  "version": "0.13-3",
  "architectures": [
    "all"
  ],
  "homepage": "https://github.com/barseghyanartur/tld",
  "repository": "https://github.com/barseghyanartur/tld",
  "source_package": "python-tld",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install python3-tld"
  },
  "packages": [
    {
      "name": "python3-tld"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package",
    "python"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use python3-tld because Extract the top level domain (TLD) from a given URL (Python 3).",
    "When the engagement is in the install phase, use python3-tld as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair python3-tld with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `python3-tld --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Extract the top level domain (TLD) from a given URL (Python 3)\nThis package contains a module which extracts the top level domain (TLD) from\nthe given URL. The list of TLD names is taken from Mozilla.\n.\nIt can optionally raises exceptions on non-existing TLDs or silently fails (if\nfail_silently argument is set to True).",
    "how_to_use": "Install/setup: sudo apt install python-tld. Extract the top level domain (TLD) from a given URL (Python 3).",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Extract the top level domain (TLD) from a given URL (Python 3).",
    "why_to_use": "Prefer this tool based on upstream description: Extract the top level domain (TLD) from a given URL (Python 3)\nThis package contains a module which extracts the top level domain (TLD) from\nthe given URL. The list of TLD names is taken from Mozilla.\n.\nIt can optionally raises exceptions on non-existing TLDs or silently fails (if\nfail_silently argument is set to True).",
    "usage_sections": [
      "Install with: sudo apt install python-tld",
      "Install with: sudo apt install python-tld"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
