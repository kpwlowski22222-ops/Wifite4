{
  "category": "exploit",
  "description": "yara-python allows you to use YARA from Python. Useful for malware analysis and detection.",
  "documentation": {
    "entry": "yara",
    "example": "import yara; rules = yara.compile(filepath='rules.yar'); rules.match('/bin/ls')",
    "import": "yara",
    "pypi": "https://pypi.org/project/yara-python/"
  },
  "full_name": "yara",
  "id": "pypi:yara-python",
  "kind": "python_library",
  "metadata": {
    "category": "exploit",
    "entry": "yara",
    "example": "import yara; rules = yara.compile(filepath='rules.yar'); rules.match('/bin/ls')",
    "import_name": "yara",
    "pip_name": "yara-python",
    "version": "4.5.1"
  },
  "metadata_status": "indexed",
  "name": "yara-python",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "YARA pattern-matching (Python bindings).",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/yara-python/",
  "tags": [
    "offensive",
    "yara",
    "python",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/yara-python && python3 yara-python.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/yara-python && python3 yara-python.py --help",
    "cd toolboxes/yara-python && python3 yara-python.py --help",
    "cd toolboxes/yara-python && python3 yara-python.py --help",
    "cd toolboxes/yara-python && python3 yara-python.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST yara-python --output $KFIOSA_OUTPUT_DIR/yara-python.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST yara-python --output $KFIOSA_OUTPUT_DIR/yara-python.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}