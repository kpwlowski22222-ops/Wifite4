{
  "id": "kali:liblerc4",
  "kind": "kali_source_package",
  "name": "liblerc4",
  "title": "liblerc4",
  "summary": "Limited Error Raster Compression library",
  "description": "Limited Error Raster Compression library\nLERC is an open-source image or raster format which\nsupports rapid encoding and decoding for any pixel\ntype (not just RGB or Byte). Users set the maximum\ncompression error per pixel while encoding, so the\nprecision of the original input image is preserved\n(within user defined error bounds).",
  "version": "4.1.0+ds-2",
  "architectures": [
    "amd64"
  ],
  "homepage": "https://github.com/Esri/lerc",
  "repository": "https://github.com/Esri/lerc",
  "source_package": "lerc",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install liblerc4"
  },
  "packages": [
    {
      "name": "liblerc4"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use liblerc4 because Limited Error Raster Compression library.",
    "When the engagement is in the install phase, use liblerc4 as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair liblerc4 with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `liblerc4 --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Limited Error Raster Compression library\nLERC is an open-source image or raster format which\nsupports rapid encoding and decoding for any pixel\ntype (not just RGB or Byte). Users set the maximum\ncompression error per pixel while encoding, so the\nprecision of the original input image is preserved\n(within user defined error bounds).",
    "how_to_use": "Install/setup: sudo apt install lerc.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Limited Error Raster Compression library.",
    "why_to_use": "Prefer this tool based on upstream description: Limited Error Raster Compression library\nLERC is an open-source image or raster format which\nsupports rapid encoding and decoding for any pixel\ntype (not just RGB or Byte). Users set the maximum\ncompression error per pixel while encoding, so the\nprecision of the original input image is preserved\n(within user defined error bounds).",
    "usage_sections": [
      "Install with: sudo apt install lerc",
      "Install with: sudo apt install lerc"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
