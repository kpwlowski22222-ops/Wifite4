{
  "id": "kali:copy-router-config",
  "kind": "kali_source_package",
  "name": "copy-router-config",
  "title": "copy-router-config",
  "summary": "Copies Cisco configs via SNMP",
  "description": "Copies Cisco configs via SNMP\nThis package copies configuration files from Cisco devices\nrunning SNMP.",
  "version": "1.0-1kali5",
  "architectures": [
    "all"
  ],
  "homepage": "https://www.offsec.com",
  "repository": "https://www.offsec.com",
  "source_package": "copy-router-config",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install copy-router-config"
  },
  "packages": [
    {
      "name": "copy-router-config"
    }
  ],
  "commands": [
    {
      "name": "copy-router-config.pl",
      "path": "/usr/bin/copy-router-config.pl",
      "description": "######################################################",
      "usage": "copy-router-config.pl [options] [args]",
      "arguments": [
        {
          "flags": [
            "-router-config"
          ],
          "metavar": null,
          "takes_value": false,
          "description": "Option present in package usage line for copy-router-config.pl.",
          "default": null,
          "choices": null,
          "raw": "-router-config  Option present in package usage line for copy-router-config.pl."
        }
      ]
    },
    {
      "name": "merge-router-config.pl",
      "path": "/usr/bin/merge-router-config.pl",
      "description": "######################################################",
      "usage": "merge-router-config.pl [options] [args]",
      "arguments": []
    }
  ],
  "files": [
    "/usr/bin/copy-router-config.pl",
    "/usr/bin/merge-router-config.pl"
  ],
  "tags": [
    "os-package",
    "networking"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "command_examples": [
    "copy-router-config.pl [options] [args]"
  ],
  "use_cases": [
    "When you need Kali Linux, use copy-router-config.pl because Copies Cisco configs via SNMP.",
    "When the engagement is in the install phase, use copy-router-config.pl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When you need a concrete invocation, run `copy-router-config.pl [options] [args]` as documented upstream for copy-router-config.pl.",
    "When chaining Kali Linux tools, pair copy-router-config.pl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `copy-router-config.pl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [
      {
        "name": "-router-config",
        "description": "Option present in package usage line for copy-router-config.pl.",
        "source": "usage_line"
      }
    ],
    "examples": [
      "copy-router-config.pl [options] [args]"
    ],
    "readme": "Copies Cisco configs via SNMP\nThis package copies configuration files from Cisco devices\nrunning SNMP.",
    "how_to_use": "Install/setup: sudo apt install copy-router-config. Run the primary entrypoint as shown in upstream docs, e.g. `copy-router-config.pl [options] [args]`.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Copies Cisco configs via SNMP.",
    "why_to_use": "Prefer this tool based on upstream description: Copies Cisco configs via SNMP\nThis package copies configuration files from Cisco devices\nrunning SNMP.",
    "usage_sections": [
      "Install with: sudo apt install copy-router-config",
      "Install with: sudo apt install copy-router-config"
    ]
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline"
}
