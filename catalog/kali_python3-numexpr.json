{
  "id": "kali:python3-numexpr",
  "kind": "kali_source_package",
  "name": "python3-numexpr",
  "title": "python3-numexpr",
  "summary": "Fast numerical array expression evaluator for Python 3 and NumPy",
  "description": "Fast numerical array expression evaluator for Python 3 and NumPy\nNumexpr package evaluates multiple-operator array expressions many\ntimes faster than NumPy can. It accepts the expression as a string,\nanalyzes it, rewrites it more efficiently, and compiles it to faster\nPython code on the fly. It's the next best thing to writing the\nexpression in C and compiling it with a specialized just-in-time\n(JIT) compiler, i.e. it does not require a compiler at runtime.",
  "version": "2.14.1-1+b1",
  "architectures": [
    "amd64"
  ],
  "homepage": "https://github.com/pydata/numexpr",
  "repository": "https://github.com/pydata/numexpr",
  "source_package": "numexpr (2.14.1-1)",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install python3-numexpr"
  },
  "packages": [
    {
      "name": "python3-numexpr"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package",
    "python"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use python3-numexpr because Fast numerical array expression evaluator for Python 3 and NumPy.",
    "When the engagement is in the install phase, use python3-numexpr as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair python3-numexpr with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `python3-numexpr --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Fast numerical array expression evaluator for Python 3 and NumPy\nNumexpr package evaluates multiple-operator array expressions many\ntimes faster than NumPy can. It accepts the expression as a string,\nanalyzes it, rewrites it more efficiently, and compiles it to faster\nPython code on the fly. It's the next best thing to writing the\nexpression in C and compiling it with a specialized just-in-time\n(JIT) compiler, i.e. it does not require a compiler at runtime.",
    "how_to_use": "Install/setup: sudo apt install numexpr (2.14.1-1). Fast numerical array expression evaluator for Python 3 and NumPy.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Fast numerical array expression evaluator for Python 3 and NumPy.",
    "why_to_use": "Prefer this tool based on upstream description: Fast numerical array expression evaluator for Python 3 and NumPy\nNumexpr package evaluates multiple-operator array expressions many\ntimes faster than NumPy can. It accepts the expression as a string,\nanalyzes it, rewrites it more efficiently, and compiles it to faster\nPython code on the fly. It's the next best thing to writing the\nexpression in C and compiling it with a specialized just-in-time\n(J.",
    "usage_sections": [
      "Install with: sudo apt install numexpr (2.14.1-1)",
      "Install with: sudo apt install numexpr (2.14.1-1)"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
