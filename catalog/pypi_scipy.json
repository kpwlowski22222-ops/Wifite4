{
  "category": "ai",
  "description": "SciPy provides algorithms for optimization, integration, interpolation, eigenvalue problems, statistical distributions, etc.",
  "documentation": {
    "entry": "scipy",
    "example": "from scipy import stats; print(stats.norm.pdf(0))",
    "import": "scipy",
    "pypi": "https://pypi.org/project/scipy/"
  },
  "full_name": "scipy",
  "id": "pypi:scipy",
  "kind": "python_library",
  "metadata": {
    "category": "ai",
    "entry": "scipy",
    "example": "from scipy import stats; print(stats.norm.pdf(0))",
    "import_name": "scipy",
    "pip_name": "scipy",
    "version": "1.13.0"
  },
  "metadata_status": "indexed",
  "name": "scipy",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "Fundamental algorithms for scientific computing.",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/scipy/",
  "tags": [
    "offensive",
    "scipy",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/scipy && python3 scipy.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/scipy && python3 scipy.py --help",
    "cd toolboxes/scipy && python3 scipy.py --help",
    "cd toolboxes/scipy && python3 scipy.py --help",
    "cd toolboxes/scipy && python3 scipy.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST scipy --output $KFIOSA_OUTPUT_DIR/scipy.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST scipy --output $KFIOSA_OUTPUT_DIR/scipy.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}