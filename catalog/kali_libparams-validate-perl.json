{
  "id": "kali:libparams-validate-perl",
  "kind": "kali_source_package",
  "name": "libparams-validate-perl",
  "title": "libparams-validate-perl",
  "summary": "Perl module to validate parameters to Perl method/function calls",
  "description": "Perl module to validate parameters to Perl method/function calls\nParams::Validate is a Perl module providing a flexible way to validate method\nand function call parameters. The validation can be as simple as checking for\nthe presence of required parameters, or more complex, like validating object\nclasses (via isa) or capabilities (via can) and checking parameter types. It\nalso provides extensibility through customized validation callbacks.\n.\nThe module has been designed to work equally well with positional or named\nparameters (via a hash or hash reference) and includes experimental support\nfor attributes (see Attribute::Params::Validate for details).",
  "version": "1.31-2+b4",
  "architectures": [
    "amd64"
  ],
  "homepage": "https://metacpan.org/release/Params-Validate",
  "repository": "https://metacpan.org/release/Params-Validate",
  "source_package": "libparams-validate-perl (1.31-2)",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libparams-validate-perl"
  },
  "packages": [
    {
      "name": "libparams-validate-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/Params::Validate.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libparams-validate-perl because Perl module to validate parameters to Perl method/function calls.",
    "When the engagement is in the install phase, use libparams-validate-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libparams-validate-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libparams-validate-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Perl module to validate parameters to Perl method/function calls\nParams::Validate is a Perl module providing a flexible way to validate method\nand function call parameters. The validation can be as simple as checking for\nthe presence of required parameters, or more complex, like validating object\nclasses (via isa) or capabilities (via can) and checking parameter types. It\nalso provides extensibility through customized validation callbacks.\n.\nThe module has been designed to work equally well with positional or named\nparameters (via a hash or hash reference) and includes experimental support\nfor attributes (see Attribute::Params::Validate for details).",
    "how_to_use": "Install/setup: sudo apt install libparams-validate-perl (1.31-2). Perl module to validate parameters to Perl method/function calls.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Perl module to validate parameters to Perl method/function calls.",
    "why_to_use": "Prefer this tool based on upstream description: Perl module to validate parameters to Perl method/function calls\nParams::Validate is a Perl module providing a flexible way to validate method\nand function call parameters. The validation can be as simple as checking for\nthe presence of required parameters, or more complex, like validating object\nclasses (via isa) or capabilities (via can) and checking parameter types. It\nalso provides extensibili.",
    "usage_sections": [
      "Install with: sudo apt install libparams-validate-perl (1.31-2)",
      "Install with: sudo apt install libparams-validate-perl (1.31-2)"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
