{
  "id": "kali:python3-zc.lockfile",
  "kind": "kali_source_package",
  "name": "python3-zc.lockfile",
  "title": "python3-zc.lockfile",
  "summary": "Basic inter-process locks for Python 3",
  "description": "Basic inter-process locks for Python 3\nThe zc.lockfile package provides a basic portable implementation of\ninterprocess locks using lock files. The purpose if not specifically to lock\nfiles, but to simply provide locks with an implementation based on\nfile-locking primitives.  Of course, these locks could be used to mediate\naccess to *other* files. For example, the ZODB file storage implementation\nuses file locks to mediate access to file-storage database files. The database\nfiles and lock file files are separate files.",
  "version": "4.0-3",
  "architectures": [
    "all"
  ],
  "homepage": "https://github.com/zopefoundation/zc.lockfile",
  "repository": "https://github.com/zopefoundation/zc.lockfile",
  "source_package": "zc.lockfile",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install python3-zc.lockfile"
  },
  "packages": [
    {
      "name": "python3-zc.lockfile"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package",
    "python"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use python3-zc.lockfile because Basic inter-process locks for Python 3.",
    "When the engagement is in the install phase, use python3-zc.lockfile as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair python3-zc.lockfile with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `python3-zc.lockfile --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Basic inter-process locks for Python 3\nThe zc.lockfile package provides a basic portable implementation of\ninterprocess locks using lock files. The purpose if not specifically to lock\nfiles, but to simply provide locks with an implementation based on\nfile-locking primitives.  Of course, these locks could be used to mediate\naccess to *other* files. For example, the ZODB file storage implementation\nuses file locks to mediate access to file-storage database files. The database\nfiles and lock file files are separate files.",
    "how_to_use": "Install/setup: sudo apt install zc.lockfile.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Basic inter-process locks for Python 3.",
    "why_to_use": "Prefer this tool based on upstream description: Basic inter-process locks for Python 3\nThe zc.lockfile package provides a basic portable implementation of\ninterprocess locks using lock files. The purpose if not specifically to lock\nfiles, but to simply provide locks with an implementation based on\nfile-locking primitives.  Of course, these locks could be used to mediate\naccess to *other* files. For example, the ZODB file storage implementation\n.",
    "usage_sections": [
      "Install with: sudo apt install zc.lockfile",
      "Install with: sudo apt install zc.lockfile"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
