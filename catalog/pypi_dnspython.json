{
  "category": "recon",
  "description": "dnspython is a DNS toolkit for Python. It supports almost all record types, can be used for zone transfers, dynamic updates, messages, TSIG, EDNS, and more.",
  "documentation": {
    "entry": "dns",
    "example": "import dns.resolver; print(dns.resolver.resolve('example.com', 'A'))",
    "import": "dns",
    "pypi": "https://pypi.org/project/dnspython/"
  },
  "full_name": "dns",
  "id": "pypi:dnspython",
  "kind": "python_library",
  "metadata": {
    "category": "recon",
    "entry": "dns",
    "example": "import dns.resolver; print(dns.resolver.resolve('example.com', 'A'))",
    "import_name": "dns",
    "pip_name": "dnspython",
    "version": "2.6.1"
  },
  "metadata_status": "indexed",
  "name": "dnspython",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "DNS toolkit for Python (resolver, zone, TSIG, ...).",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/dnspython/",
  "tags": [
    "offensive",
    "dnspython",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/dnspython && python3 dnspython.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/dnspython && python3 dnspython.py --help",
    "cd toolboxes/dnspython && python3 dnspython.py --help",
    "cd toolboxes/dnspython && python3 dnspython.py --help",
    "cd toolboxes/dnspython && python3 dnspython.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST dnspython --output $KFIOSA_OUTPUT_DIR/dnspython.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST dnspython --output $KFIOSA_OUTPUT_DIR/dnspython.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}