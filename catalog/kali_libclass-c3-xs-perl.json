{
  "id": "kali:libclass-c3-xs-perl",
  "kind": "kali_source_package",
  "name": "libclass-c3-xs-perl",
  "title": "libclass-c3-xs-perl",
  "summary": "Perl module to accelerate Class::C3",
  "description": "Perl module to accelerate Class::C3\nClass::C3::XS is a Perl module designed to enhance the performance of the\nClass::C3 package (see libclass-c3-perl). It does so automatically and is not\ndesigned to be used directly; once installed, it should accelerate all programs\nthat make use of Class::C3 itself.",
  "version": "0.15-1+b6",
  "architectures": [
    "amd64"
  ],
  "homepage": "https://metacpan.org/release/Class-C3-XS",
  "repository": "https://metacpan.org/release/Class-C3-XS",
  "source_package": "libclass-c3-xs-perl (0.15-1)",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libclass-c3-xs-perl"
  },
  "packages": [
    {
      "name": "libclass-c3-xs-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/Class::C3::XS.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libclass-c3-xs-perl because Perl module to accelerate Class::C3.",
    "When the engagement is in the install phase, use libclass-c3-xs-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libclass-c3-xs-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libclass-c3-xs-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Perl module to accelerate Class::C3\nClass::C3::XS is a Perl module designed to enhance the performance of the\nClass::C3 package (see libclass-c3-perl). It does so automatically and is not\ndesigned to be used directly; once installed, it should accelerate all programs\nthat make use of Class::C3 itself.",
    "how_to_use": "Install/setup: sudo apt install libclass-c3-xs-perl (0.15-1).",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Perl module to accelerate Class::C3.",
    "why_to_use": "Prefer this tool based on upstream description: Perl module to accelerate Class::C3\nClass::C3::XS is a Perl module designed to enhance the performance of the\nClass::C3 package (see libclass-c3-perl). It does so automatically and is not\ndesigned to be used directly; once installed, it should accelerate all programs\nthat make use of Class::C3 itself.",
    "usage_sections": [
      "Install with: sudo apt install libclass-c3-xs-perl (0.15-1)",
      "Install with: sudo apt install libclass-c3-xs-perl (0.15-1)"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
