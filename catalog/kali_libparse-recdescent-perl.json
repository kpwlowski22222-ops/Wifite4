{
  "id": "kali:libparse-recdescent-perl",
  "kind": "kali_source_package",
  "name": "libparse-recdescent-perl",
  "title": "libparse-recdescent-perl",
  "summary": "Perl module to create and use recursive-descent parsers",
  "description": "Perl module to create and use recursive-descent parsers\nParse::RecDescent incrementally generates top-down recursive-descent text\nparsers from simple yacc-like grammar specifications. It provides:\n.\n* Regular expressions or literal strings as terminals (tokens)\n* Multiple (non-contiguous) productions for any rule\n* Repeated, optional and alternate sub-rules within productions\n* Late-bound (run-time dispatched) sub-rules\n* Full access to Perl within actions specified as part of the grammar\n* Simple automated error reporting during parser generation and parsing\n* The ability to commit to, uncommit to, or reject particular productions\nduring a parse\n* Incremental extension of the parsing grammar (even during a parse)\n* The ability to retrieve the generated parsing code",
  "version": "1.967015+dfsg-4",
  "architectures": [
    "all"
  ],
  "homepage": "https://metacpan.org/release/Parse-RecDescent/",
  "repository": "https://metacpan.org/release/Parse-RecDescent/",
  "source_package": "libparse-recdescent-perl",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libparse-recdescent-perl"
  },
  "packages": [
    {
      "name": "libparse-recdescent-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/Parse::RecDescent.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libparse-recdescent-perl because Perl module to create and use recursive-descent parsers.",
    "When the engagement is in the install phase, use libparse-recdescent-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libparse-recdescent-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libparse-recdescent-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Perl module to create and use recursive-descent parsers\nParse::RecDescent incrementally generates top-down recursive-descent text\nparsers from simple yacc-like grammar specifications. It provides:\n.\n* Regular expressions or literal strings as terminals (tokens)\n* Multiple (non-contiguous) productions for any rule\n* Repeated, optional and alternate sub-rules within productions\n* Late-bound (run-time dispatched) sub-rules\n* Full access to Perl within actions specified as part of the grammar\n* Simple automated error reporting during parser generation and parsing\n* The ability to commit to, uncommit to, or reject particular productions\nduring a parse\n* Incremental extension of the parsing grammar (even during a parse)\n* The ability to retrieve the generated parsing code",
    "how_to_use": "Install/setup: sudo apt install libparse-recdescent-perl. Perl module to create and use recursive-descent parsers.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Perl module to create and use recursive-descent parsers.",
    "why_to_use": "Prefer this tool based on upstream description: Perl module to create and use recursive-descent parsers\nParse::RecDescent incrementally generates top-down recursive-descent text\nparsers from simple yacc-like grammar specifications. It provides:\n.\n* Regular expressions or literal strings as terminals (tokens)\n* Multiple (non-contiguous) productions for any rule\n* Repeated, optional and alternate sub-rules within productions\n* Late-bound (run-tim.",
    "usage_sections": [
      "Install with: sudo apt install libparse-recdescent-perl",
      "Install with: sudo apt install libparse-recdescent-perl"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
