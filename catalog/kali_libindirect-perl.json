{
  "id": "kali:libindirect-perl",
  "kind": "kali_source_package",
  "name": "libindirect-perl",
  "title": "libindirect-perl",
  "summary": "module warning about using the indirect object syntax",
  "description": "module warning about using the indirect object syntax\nWhen enabled (or disabled as some may prefer to say, since you actually turn\nit on by calling no indirect), the indirect pragma lexically warns about\nindirect object syntax constructs that may have slipped into your code. This\nsyntax is now considered harmful, since its parsing has many quirks and its\nuse is error prone (when swoosh isn't defined, swoosh $x actually compiles to\n$x->swoosh).\n.\nIt currently does not warn for core functions (print, say, exec or system).\nThis may change in the future, or may be added as optional features that\nwould be enabled by passing options to unimport.\n.\nindirect is not a source filter.",
  "version": "0.39-2+b4",
  "architectures": [
    "amd64"
  ],
  "homepage": "https://metacpan.org/release/indirect",
  "repository": "https://metacpan.org/release/indirect",
  "source_package": "libindirect-perl (0.39-2)",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libindirect-perl"
  },
  "packages": [
    {
      "name": "libindirect-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/indirect.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libindirect-perl because module warning about using the indirect object syntax.",
    "When the engagement is in the install phase, use libindirect-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libindirect-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libindirect-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "module warning about using the indirect object syntax\nWhen enabled (or disabled as some may prefer to say, since you actually turn\nit on by calling no indirect), the indirect pragma lexically warns about\nindirect object syntax constructs that may have slipped into your code. This\nsyntax is now considered harmful, since its parsing has many quirks and its\nuse is error prone (when swoosh isn't defined, swoosh $x actually compiles to\n$x->swoosh).\n.\nIt currently does not warn for core functions (print, say, exec or system).\nThis may change in the future, or may be added as optional features that\nwould be enabled by passing options to unimport.\n.\nindirect is not a source filter.",
    "how_to_use": "Install/setup: sudo apt install libindirect-perl (0.39-2). module warning about using the indirect object syntax.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: module warning about using the indirect object syntax.",
    "why_to_use": "Prefer this tool based on upstream description: module warning about using the indirect object syntax\nWhen enabled (or disabled as some may prefer to say, since you actually turn\nit on by calling no indirect), the indirect pragma lexically warns about\nindirect object syntax constructs that may have slipped into your code. This\nsyntax is now considered harmful, since its parsing has many quirks and its\nuse is error prone (when swoosh isn't defin.",
    "usage_sections": [
      "Install with: sudo apt install libindirect-perl (0.39-2)",
      "Install with: sudo apt install libindirect-perl (0.39-2)"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
