{
  "id": "kali:comerr-dev",
  "kind": "kali_source_package",
  "name": "comerr-dev",
  "title": "comerr-dev",
  "summary": "common error description library - headers and static libraries",
  "description": "common error description library - headers and static libraries\nlibcom_err is an attempt to present a common error-handling mechanism to\nmanipulate the most common form of error code in a fashion that does not\nhave the problems identified with mechanisms commonly in use.\n.\nThis package contains the development environment for the com_err library.",
  "version": "2.1-1.47.4-1",
  "architectures": [
    "amd64"
  ],
  "homepage": "http://e2fsprogs.sourceforge.net",
  "repository": "http://e2fsprogs.sourceforge.net",
  "source_package": "e2fsprogs (1.47.4-1)",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install comerr-dev"
  },
  "packages": [
    {
      "name": "comerr-dev"
    }
  ],
  "commands": [
    {
      "name": "compile_et",
      "path": "/usr/bin/compile_et",
      "description": "error table compiler",
      "usage": "compile_et [options] [args]",
      "arguments": [
        {
          "flags": [
            "-code"
          ],
          "metavar": null,
          "takes_value": false,
          "description": "followed by up to 256 entries of the form:",
          "default": null,
          "choices": null,
          "raw": "-code  followed by up to 256 entries of the form:"
        }
      ]
    }
  ],
  "files": [
    "/usr/bin/compile_et",
    "/usr/share/man/man1/compile_et.1.gz",
    "/usr/share/man/man3/com_err.3.gz"
  ],
  "tags": [
    "os-package"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "command_examples": [
    "compile_et file"
  ],
  "use_cases": [
    "When you need Kali Linux, use compile_et because common error description library - headers and static libraries.",
    "When the engagement is in the install phase, use compile_et as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When you need a concrete invocation, run `compile_et file` as documented upstream for compile_et.",
    "When chaining Kali Linux tools, pair compile_et with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `compile_et --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [
      {
        "name": "-code",
        "description": "followed by up to 256 entries of the form:",
        "source": "manpage"
      }
    ],
    "examples": [
      "compile_et file"
    ],
    "readme": "compile_et - error table compiler",
    "how_to_use": "Install/setup: sudo apt install e2fsprogs (1.47.4-1). common error description library - headers and static libraries.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: common error description library - headers and static libraries.",
    "why_to_use": "Prefer this tool because: common error description library - headers and static libraries.",
    "usage_sections": [
      "Install with: sudo apt install e2fsprogs (1.47.4-1)",
      "Install with: sudo apt install e2fsprogs (1.47.4-1)"
    ]
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "local_man"
}
