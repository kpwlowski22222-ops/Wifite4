{
  "category": "crypto",
  "description": "PyCryptodome is a self-contained Python package of low-level cryptographic primitives. Supports AES, RSA, DES, 3DES, hashing, HMAC, PBKDF2, scrypt, etc.",
  "documentation": {
    "entry": "Crypto",
    "example": "from Crypto.Cipher import AES; print(AES.new(b'0'*16, AES.MODE_ECB).encrypt(b'0'*16).hex())",
    "import": "Crypto",
    "pypi": "https://pypi.org/project/pycryptodome/"
  },
  "full_name": "Crypto",
  "id": "pypi:pycryptodome",
  "kind": "python_library",
  "metadata": {
    "category": "crypto",
    "entry": "Crypto",
    "example": "from Crypto.Cipher import AES; print(AES.new(b'0'*16, AES.MODE_ECB).encrypt(b'0'*16).hex())",
    "import_name": "Crypto",
    "pip_name": "pycryptodome",
    "version": "3.20.0"
  },
  "metadata_status": "indexed",
  "name": "pycryptodome",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "Self-contained cryptographic library (AES, RSA, ...).",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/pycryptodome/",
  "tags": [
    "offensive",
    "pycryptodome",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/pycryptodome && python3 pycryptodome.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/pycryptodome && python3 pycryptodome.py --help",
    "cd toolboxes/pycryptodome && python3 pycryptodome.py --help",
    "cd toolboxes/pycryptodome && python3 pycryptodome.py --help",
    "cd toolboxes/pycryptodome && python3 pycryptodome.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST pycryptodome --output $KFIOSA_OUTPUT_DIR/pycryptodome.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST pycryptodome --output $KFIOSA_OUTPUT_DIR/pycryptodome.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}