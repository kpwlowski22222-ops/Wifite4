{
  "id": "kali:python3-monotonic",
  "kind": "kali_source_package",
  "name": "python3-monotonic",
  "title": "python3-monotonic",
  "summary": "implementation of time.monotonic() - Python 3.x",
  "description": "implementation of time.monotonic() - Python 3.x\nThis module provides a monotonic() function which returns the value (in\nfractional seconds) of a clock which never goes backwards. On Python 3.3 or\nnewer, monotonic will be an alias of time.monotonic from the standard library.\nOn older versions, it will fall back to an equivalent implementation:\nGetTickCount64 on Windows, mach_absolute_time on OS X, and clock_gettime(3)\non Linux/BSD.\n.\nIf no suitable implementation exists for the current platform, attempting to\nimport this module (or to import from it) will cause a RuntimeError exception\nto be raised.\n.\nThis package contains the Python 3.x module.",
  "version": "1.6-3",
  "architectures": [
    "all"
  ],
  "homepage": "https://github.com/atdt/monotonic",
  "repository": "https://github.com/atdt/monotonic",
  "source_package": "python-monotonic",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install python3-monotonic"
  },
  "packages": [
    {
      "name": "python3-monotonic"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package",
    "python"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use python3-monotonic because implementation of time.monotonic() - Python 3.x.",
    "When the engagement is in the install phase, use python3-monotonic as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair python3-monotonic with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `python3-monotonic --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "implementation of time.monotonic() - Python 3.x\nThis module provides a monotonic() function which returns the value (in\nfractional seconds) of a clock which never goes backwards. On Python 3.3 or\nnewer, monotonic will be an alias of time.monotonic from the standard library.\nOn older versions, it will fall back to an equivalent implementation:\nGetTickCount64 on Windows, mach_absolute_time on OS X, and clock_gettime(3)\non Linux/BSD.\n.\nIf no suitable implementation exists for the current platform, attempting to\nimport this module (or to import from it) will cause a RuntimeError exception\nto be raised.\n.\nThis package contains the Python 3.x module.",
    "how_to_use": "Install/setup: sudo apt install python-monotonic. implementation of time.monotonic() - Python 3.x.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: implementation of time.monotonic() - Python 3.x.",
    "why_to_use": "Prefer this tool based on upstream description: implementation of time.monotonic() - Python 3.x\nThis module provides a monotonic() function which returns the value (in\nfractional seconds) of a clock which never goes backwards. On Python 3.3 or\nnewer, monotonic will be an alias of time.monotonic from the standard library.\nOn older versions, it will fall back to an equivalent implementation:\nGetTickCount64 on Windows, mach_absolute_time on OS X, .",
    "usage_sections": [
      "Install with: sudo apt install python-monotonic",
      "Install with: sudo apt install python-monotonic"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
