{
  "id": "kali:libnet-snmp-perl",
  "kind": "kali_source_package",
  "name": "libnet-snmp-perl",
  "title": "libnet-snmp-perl",
  "summary": "Script SNMP connections",
  "description": "Script SNMP connections\nThe module Net::SNMP implements an object oriented interface to\nthe Simple Network Management Protocol.  Perl applications can\nuse the module to retrieve or update information on a remote\nhost using the SNMP protocol.  Net::SNMP is implemented completely\nin Perl, requires no compiling, and uses only standard Perl\nmodules.  SNMPv1 and SNMPv2c (Community-Based SNMPv2), as well as\nSNMPv3 with USM are supported by the module.  SNMP over UDP as well\nas TCP with both IPv4 and IPv6 can be used.  The Net::SNMP module\nassumes that the user has a basic understanding of the Simple\nNetwork Management Protocol and related network management concepts.",
  "version": "6.0.1-8",
  "architectures": [
    "all"
  ],
  "homepage": "https://metacpan.org/release/Net-SNMP",
  "repository": "https://metacpan.org/release/Net-SNMP",
  "source_package": "libnet-snmp-perl",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libnet-snmp-perl"
  },
  "packages": [
    {
      "name": "libnet-snmp-perl"
    }
  ],
  "commands": [
    {
      "name": "snmpkey",
      "path": "/usr/bin/snmpkey",
      "description": "Create SNMPv3 security keys for the Net::SNMP module",
      "usage": "snmpkey [options] [args]",
      "arguments": [
        {
          "flags": [
            "-03-01"
          ],
          "metavar": null,
          "takes_value": false,
          "description": "SNMPKEY(1p)",
          "default": null,
          "choices": null,
          "raw": "-03-01  SNMPKEY(1p)"
        },
        {
          "flags": [
            "-2009"
          ],
          "metavar": null,
          "takes_value": false,
          "description": "All rights reserved.",
          "default": null,
          "choices": null,
          "raw": "-2009  All rights reserved."
        },
        {
          "flags": [
            "-MD5-96"
          ],
          "metavar": null,
          "takes_value": false,
          "description": "be specified with the string 'md5' or 'sha' respectively. This choice must",
          "default": null,
          "choices": null,
          "raw": "-MD5-96  be specified with the string 'md5' or 'sha' respectively. This choice must"
        },
        {
          "flags": [
            "-authkey"
          ],
          "metavar": null,
          "takes_value": false,
          "description": "arguments",
          "default": null,
          "choices": null,
          "raw": "-authkey  arguments"
        },
        {
          "flags": [
            "-based"
          ],
          "metavar": null,
          "takes_value": false,
          "description": "Net::SNMP module supports",
          "default": null,
          "choices": null,
          "raw": "-based  Net::SNMP module supports"
        },
        {
          "flags": [
            "-privprotocol"
          ],
          "metavar": null,
          "takes_value": false,
          "description": "the Net::SNMP object.",
          "default": null,
          "choices": null,
          "raw": "-privprotocol  the Net::SNMP object."
        },
        {
          "flags": [
            "-way"
          ],
          "metavar": null,
          "takes_value": false,
          "description": "This resulting key is",
          "default": null,
          "choices": null,
          "raw": "-way  This resulting key is"
        }
      ]
    }
  ],
  "files": [
    "/usr/bin/snmpkey",
    "/usr/share/man/man1/snmpkey.1p.gz",
    "/usr/share/man/man3/Net::SNMP.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "command_examples": [
    "snmpkey <authProto> <password> <authEngineID> [<privProto> [<password>]]"
  ],
  "use_cases": [
    "When you need Kali Linux, use snmpkey because Script SNMP connections.",
    "When the engagement is in the install phase, use snmpkey as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When you need a concrete invocation, run `snmpkey <authProto> <password> <authEngineID> [<privProto> [<password>]]` as documented upstream for snmpkey.",
    "When chaining Kali Linux tools, pair snmpkey with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `snmpkey --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [
      {
        "name": "-03-01",
        "description": "SNMPKEY(1p)",
        "source": "manpage"
      },
      {
        "name": "-2009",
        "description": "All rights reserved.",
        "source": "manpage"
      },
      {
        "name": "-MD5-96",
        "description": "be specified with the string 'md5' or 'sha' respectively. This choice must",
        "source": "manpage"
      },
      {
        "name": "-authkey",
        "description": "arguments",
        "source": "manpage"
      },
      {
        "name": "-based",
        "description": "Net::SNMP module supports",
        "source": "manpage"
      },
      {
        "name": "-privprotocol",
        "description": "the Net::SNMP object.",
        "source": "manpage"
      },
      {
        "name": "-way",
        "description": "This resulting key is",
        "source": "manpage"
      }
    ],
    "examples": [
      "snmpkey <authProto> <password> <authEngineID> [<privProto> [<password>]]"
    ],
    "readme": "snmpkey - Create SNMPv3 security keys for the Net::SNMP module USAGE The \"snmpkey\" utility generates security keys based on a password and an authoritativeEngineID passed on the command line. This key can then be used by the Net::SNMP module instead of the plain text password when creating SNMPv3 objects. snmpkey <authProto> <password> <authEngineID> [<privProto> [<password>]]",
    "how_to_use": "Install/setup: sudo apt install libnet-snmp-perl. Run the primary entrypoint as shown in upstream docs, e.g. `snmpkey <authProto> <password> <authEngineID> [<privProto> [<password>]]`.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Script SNMP connections.",
    "why_to_use": "Prefer this tool based on upstream description: snmpkey - Create SNMPv3 security keys for the Net::SNMP module USAGE The \"snmpkey\" utility generates security keys based on a password and an authoritativeEngineID passed on the command line. This key can then be used by the Net::SNMP module instead of the plain text password when creating SNMPv3 objects. snmpkey <authProto> <password> <authEngineID> [<privProto> [<password>]].",
    "usage_sections": [
      "Install with: sudo apt install libnet-snmp-perl",
      "Install with: sudo apt install libnet-snmp-perl"
    ]
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "local_man"
}
