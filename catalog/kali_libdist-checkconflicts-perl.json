{
  "id": "kali:libdist-checkconflicts-perl",
  "kind": "kali_source_package",
  "name": "libdist-checkconflicts-perl",
  "title": "libdist-checkconflicts-perl",
  "summary": "module for declaring version conflicts for a distribution",
  "description": "module for declaring version conflicts for a distribution\nOne shortcoming of the CPAN clients that currently exist is that they have no\nway of specifying conflicting downstream dependencies of modules.\nDist::CheckConflicts attempts to work around this issue by allowing you to\nspecify conflicting versions of modules separately, and deal with them after\nthe module is done installing.\n.\nFor instance, say you have a module Foo, and some other module Bar uses Foo.\nIf Foo were to change its API in a non-backwards-compatible way, this would\ncause Bar to break until it is updated to use the new API. Foo can't just\ndepend on the fixed version of Bar, because this will cause a circular\ndependency (because Bar is already depending on Foo), and this doesn't\nexpress intent properly anyway - Foo doesn't use Bar at all. The ideal\nsolution would be for there to be a way to specify conflicting versions of\nmodules in a way that would let CPAN clients update conflicting modules\nautomatically after an existing module is upgraded, but until that happens,\nthis module will allow users to do this manually.",
  "version": "0.11-2",
  "architectures": [
    "all"
  ],
  "homepage": "https://metacpan.org/release/Dist-CheckConflicts",
  "repository": "https://metacpan.org/release/Dist-CheckConflicts",
  "source_package": "libdist-checkconflicts-perl",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libdist-checkconflicts-perl"
  },
  "packages": [
    {
      "name": "libdist-checkconflicts-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/Dist::CheckConflicts.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libdist-checkconflicts-perl because module for declaring version conflicts for a distribution.",
    "When the engagement is in the install phase, use libdist-checkconflicts-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libdist-checkconflicts-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libdist-checkconflicts-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "module for declaring version conflicts for a distribution\nOne shortcoming of the CPAN clients that currently exist is that they have no\nway of specifying conflicting downstream dependencies of modules.\nDist::CheckConflicts attempts to work around this issue by allowing you to\nspecify conflicting versions of modules separately, and deal with them after\nthe module is done installing.\n.\nFor instance, say you have a module Foo, and some other module Bar uses Foo.\nIf Foo were to change its API in a non-backwards-compatible way, this would\ncause Bar to break until it is updated to use the new API. Foo can't just\ndepend on the fixed version of Bar, because this will cause a circular\ndependency (because Bar is already depending on Foo), and this doesn't\nexpress intent properly anyway - Foo doesn't use Bar at all. The ideal\nsolution would be for there to be a way to specify conflicting versions of\nmodules in a way that would let CPAN clients update conflicting modules\nautomatically after an existing module is upgraded, but until that happens,\nthis module will allow users to do this manually.",
    "how_to_use": "Install/setup: sudo apt install libdist-checkconflicts-perl. module for declaring version conflicts for a distribution.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: module for declaring version conflicts for a distribution.",
    "why_to_use": "Prefer this tool based on upstream description: module for declaring version conflicts for a distribution\nOne shortcoming of the CPAN clients that currently exist is that they have no\nway of specifying conflicting downstream dependencies of modules.\nDist::CheckConflicts attempts to work around this issue by allowing you to\nspecify conflicting versions of modules separately, and deal with them after\nthe module is done installing.\n.\nFor instance,.",
    "usage_sections": [
      "Install with: sudo apt install libdist-checkconflicts-perl",
      "Install with: sudo apt install libdist-checkconflicts-perl"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
