{
  "id": "kali:node-encoding",
  "kind": "kali_source_package",
  "name": "node-encoding",
  "title": "node-encoding",
  "summary": "Convert encodings, uses iconv by default and fallbacks to iconv-lite if needed",
  "description": "Convert encodings, uses iconv by default and fallbacks to iconv-lite if needed\nencoding is a simple wrapper around\nnode-iconv (https://github.com/bnoordhuis/node-iconv) and\niconv-lite (https://github.com/ashtuchkin/iconv-lite/) to convert strings\nfrom one encoding to another. If node-iconv is not available for some reason,\niconv-lite will be used instead of it as a fallback.\n.\nNode.js is an event-based server-side JavaScript engine.",
  "version": "0.1.13-2",
  "architectures": [
    "all"
  ],
  "homepage": "https://github.com/andris9/encoding",
  "repository": "https://github.com/andris9/encoding",
  "source_package": "node-encoding",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install node-encoding"
  },
  "packages": [
    {
      "name": "node-encoding"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use node-encoding because Convert encodings, uses iconv by default and fallbacks to iconv-lite if needed.",
    "When the engagement is in the install phase, use node-encoding as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair node-encoding with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `node-encoding --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Convert encodings, uses iconv by default and fallbacks to iconv-lite if needed\nencoding is a simple wrapper around\nnode-iconv (https://github.com/bnoordhuis/node-iconv) and\niconv-lite (https://github.com/ashtuchkin/iconv-lite/) to convert strings\nfrom one encoding to another. If node-iconv is not available for some reason,\niconv-lite will be used instead of it as a fallback.\n.\nNode.js is an event-based server-side JavaScript engine.",
    "how_to_use": "Install/setup: sudo apt install node-encoding. Convert encodings, uses iconv by default and fallbacks to iconv-lite if needed.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Convert encodings, uses iconv by default and fallbacks to iconv-lite if needed.",
    "why_to_use": "Prefer this tool based on upstream description: Convert encodings, uses iconv by default and fallbacks to iconv-lite if needed\nencoding is a simple wrapper around\nnode-iconv (https://github.com/bnoordhuis/node-iconv) and\niconv-lite (https://github.com/ashtuchkin/iconv-lite/) to convert strings\nfrom one encoding to another. If node-iconv is not available for some reason,\niconv-lite will be used instead of it as a fallback.\n.\nNode.js is an event-.",
    "usage_sections": [
      "Install with: sudo apt install node-encoding",
      "Install with: sudo apt install node-encoding"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
