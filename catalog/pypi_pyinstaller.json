{
  "category": "utility",
  "description": "PyInstaller bundles a Python application and all its dependencies into a single package (folder or executable).",
  "documentation": {
    "entry": "PyInstaller",
    "example": "pyinstaller --onefile myapp.py",
    "import": "PyInstaller",
    "pypi": "https://pypi.org/project/pyinstaller/"
  },
  "full_name": "PyInstaller",
  "id": "pypi:pyinstaller",
  "kind": "python_library",
  "metadata": {
    "category": "utility",
    "entry": "PyInstaller",
    "example": "pyinstaller --onefile myapp.py",
    "import_name": "PyInstaller",
    "pip_name": "pyinstaller",
    "version": "6.10.0"
  },
  "metadata_status": "indexed",
  "name": "pyinstaller",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "Bundle a Python app into a single executable.",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/pyinstaller/",
  "tags": [
    "offensive",
    "pyinstaller",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/pyinstaller && python3 pyinstaller.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/pyinstaller && python3 pyinstaller.py --help",
    "cd toolboxes/pyinstaller && python3 pyinstaller.py --help",
    "cd toolboxes/pyinstaller && python3 pyinstaller.py --help",
    "cd toolboxes/pyinstaller && python3 pyinstaller.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST pyinstaller --output $KFIOSA_OUTPUT_DIR/pyinstaller.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST pyinstaller --output $KFIOSA_OUTPUT_DIR/pyinstaller.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}