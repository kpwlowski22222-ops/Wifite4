{
  "category": "exploit",
  "description": "r2pipe is a Python client for radare2, the reverse engineering framework.",
  "documentation": {
    "entry": "r2pipe",
    "example": "import r2pipe; r2 = r2pipe.open('/bin/ls'); print(r2.cmd('iI'))",
    "import": "r2pipe",
    "pypi": "https://pypi.org/project/r2pipe/"
  },
  "full_name": "r2pipe",
  "id": "pypi:r2pipe",
  "kind": "python_library",
  "metadata": {
    "category": "exploit",
    "entry": "r2pipe",
    "example": "import r2pipe; r2 = r2pipe.open('/bin/ls'); print(r2.cmd('iI'))",
    "import_name": "r2pipe",
    "pip_name": "r2pipe",
    "version": "1.9.4"
  },
  "metadata_status": "indexed",
  "name": "r2pipe",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "Radare2 reverse engineering framework (Python bindings).",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/r2pipe/",
  "tags": [
    "offensive",
    "r2pipe",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/r2pipe && python3 r2pipe.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/r2pipe && python3 r2pipe.py --help",
    "cd toolboxes/r2pipe && python3 r2pipe.py --help",
    "cd toolboxes/r2pipe && python3 r2pipe.py --help",
    "cd toolboxes/r2pipe && python3 r2pipe.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST r2pipe --output $KFIOSA_OUTPUT_DIR/r2pipe.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST r2pipe --output $KFIOSA_OUTPUT_DIR/r2pipe.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}