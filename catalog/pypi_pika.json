{
  "category": "network",
  "description": "Pika is a pure-Python implementation of the AMQP 0-9-1 protocol used by RabbitMQ. Useful for C2 channels.",
  "documentation": {
    "entry": "pika",
    "example": "import pika; conn = pika.BlockingConnection(pika.ConnectionParameters('10.0.0.5'))",
    "import": "pika",
    "pypi": "https://pypi.org/project/pika/"
  },
  "full_name": "pika",
  "id": "pypi:pika",
  "kind": "python_library",
  "metadata": {
    "category": "network",
    "entry": "pika",
    "example": "import pika; conn = pika.BlockingConnection(pika.ConnectionParameters('10.0.0.5'))",
    "import_name": "pika",
    "pip_name": "pika",
    "version": "1.3.2"
  },
  "metadata_status": "indexed",
  "name": "pika",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": false,
    "level": "medium",
    "requires_explicit_authorization": true,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "Pure-Python AMQP 0-9-1 client (RabbitMQ).",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/pika/",
  "tags": [
    "offensive",
    "pika",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/pika && python3 pika.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/pika && python3 pika.py --help",
    "cd toolboxes/pika && python3 pika.py --help",
    "cd toolboxes/pika && python3 pika.py --help",
    "cd toolboxes/pika && python3 pika.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST pika --output $KFIOSA_OUTPUT_DIR/pika.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST pika --output $KFIOSA_OUTPUT_DIR/pika.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}