{
  "id": "kali:python3-setproctitle",
  "kind": "kali_source_package",
  "name": "python3-setproctitle",
  "title": "python3-setproctitle",
  "summary": "Setproctitle implementation for Python 3",
  "description": "Setproctitle implementation for Python 3\nThe library allows a process to change its title (as displayed by system tools\nsuch as ps and top).\n.\nChanging the title is mostly useful in multi-process systems, for example when\na master process is forked: changing the children's title allows one to\nidentify the task each process is busy with. The technique is used by\nPostgreSQL and the OpenSSH Server for example.\n.\nThis is the Python 3 package.",
  "version": "1.3.7-2",
  "architectures": [
    "amd64"
  ],
  "homepage": "https://github.com/dvarrazzo/py-setproctitle",
  "repository": "https://github.com/dvarrazzo/py-setproctitle",
  "source_package": "python-setproctitle",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install python3-setproctitle"
  },
  "packages": [
    {
      "name": "python3-setproctitle"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package",
    "python"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use python3-setproctitle because Setproctitle implementation for Python 3.",
    "When the engagement is in the install phase, use python3-setproctitle as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair python3-setproctitle with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `python3-setproctitle --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Setproctitle implementation for Python 3\nThe library allows a process to change its title (as displayed by system tools\nsuch as ps and top).\n.\nChanging the title is mostly useful in multi-process systems, for example when\na master process is forked: changing the children's title allows one to\nidentify the task each process is busy with. The technique is used by\nPostgreSQL and the OpenSSH Server for example.\n.\nThis is the Python 3 package.",
    "how_to_use": "Install/setup: sudo apt install python-setproctitle.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Setproctitle implementation for Python 3.",
    "why_to_use": "Prefer this tool based on upstream description: Setproctitle implementation for Python 3\nThe library allows a process to change its title (as displayed by system tools\nsuch as ps and top).\n.\nChanging the title is mostly useful in multi-process systems, for example when\na master process is forked: changing the children's title allows one to\nidentify the task each process is busy with. The technique is used by\nPostgreSQL and the OpenSSH Server fo.",
    "usage_sections": [
      "Install with: sudo apt install python-setproctitle",
      "Install with: sudo apt install python-setproctitle"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
