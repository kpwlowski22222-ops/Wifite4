{
  "id": "kali:libfile-listing-perl",
  "kind": "kali_source_package",
  "name": "libfile-listing-perl",
  "title": "libfile-listing-perl",
  "summary": "module to parse directory listings",
  "description": "module to parse directory listings\nFile::Listing exports a single function called parse_dir(), which can be used\nto parse directory listings.\n.\nThe first parameter to parse_dir() is the directory listing to parse. It can\nbe a scalar, a reference to an array of directory lines or a glob\nrepresenting a filehandle to read the directory listing from.\n.\nThe second parameter is the time zone to use when parsing time stamps in the\nlisting. If this value is undefined, then the local time zone is assumed.\n.\nThe third parameter is the type of listing to assume. Currently supported\nformats are 'unix', 'apache' and 'dosftp'. The default value 'unix'.",
  "version": "6.16-1",
  "architectures": [
    "all"
  ],
  "homepage": "https://metacpan.org/release/File-Listing",
  "repository": "https://metacpan.org/release/File-Listing",
  "source_package": "libfile-listing-perl",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libfile-listing-perl"
  },
  "packages": [
    {
      "name": "libfile-listing-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/File::Listing.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libfile-listing-perl because module to parse directory listings.",
    "When the engagement is in the install phase, use libfile-listing-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libfile-listing-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libfile-listing-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "module to parse directory listings\nFile::Listing exports a single function called parse_dir(), which can be used\nto parse directory listings.\n.\nThe first parameter to parse_dir() is the directory listing to parse. It can\nbe a scalar, a reference to an array of directory lines or a glob\nrepresenting a filehandle to read the directory listing from.\n.\nThe second parameter is the time zone to use when parsing time stamps in the\nlisting. If this value is undefined, then the local time zone is assumed.\n.\nThe third parameter is the type of listing to assume. Currently supported\nformats are 'unix', 'apache' and 'dosftp'. The default value 'unix'.",
    "how_to_use": "Install/setup: sudo apt install libfile-listing-perl.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: module to parse directory listings.",
    "why_to_use": "Prefer this tool based on upstream description: module to parse directory listings\nFile::Listing exports a single function called parse_dir(), which can be used\nto parse directory listings.\n.\nThe first parameter to parse_dir() is the directory listing to parse. It can\nbe a scalar, a reference to an array of directory lines or a glob\nrepresenting a filehandle to read the directory listing from.\n.\nThe second parameter is the time zone to use when.",
    "usage_sections": [
      "Install with: sudo apt install libfile-listing-perl",
      "Install with: sudo apt install libfile-listing-perl"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
