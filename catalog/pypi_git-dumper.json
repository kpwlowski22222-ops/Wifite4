{
  "category": "osint",
  "description": "git-dumper is a tool to dump a git repository from a website that exposes the .git directory.",
  "documentation": {
    "entry": "git_dumper",
    "example": "git-dumper https://example.com/.git/ ./dump",
    "import": "git_dumper",
    "pypi": "https://pypi.org/project/git-dumper/"
  },
  "full_name": "git_dumper",
  "id": "pypi:git-dumper",
  "kind": "python_library",
  "metadata": {
    "category": "osint",
    "entry": "git_dumper",
    "example": "git-dumper https://example.com/.git/ ./dump",
    "import_name": "git_dumper",
    "pip_name": "git-dumper",
    "version": "1.0.6"
  },
  "metadata_status": "indexed",
  "name": "git-dumper",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "Dump exposed .git directories.",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/git-dumper/",
  "tags": [
    "offensive",
    "git",
    "dumper",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/git-dumper && python3 git-dumper.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/git-dumper && python3 git-dumper.py --help",
    "cd toolboxes/git-dumper && python3 git-dumper.py --help",
    "cd toolboxes/git-dumper && python3 git-dumper.py --help",
    "cd toolboxes/git-dumper && python3 git-dumper.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST git-dumper --output $KFIOSA_OUTPUT_DIR/git-dumper.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST git-dumper --output $KFIOSA_OUTPUT_DIR/git-dumper.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}