{
  "category": "crypto",
  "description": "Passlib is a password hashing library for Python, providing cross-platform implementations of over 30 password hashing algorithms.",
  "documentation": {
    "entry": "passlib",
    "example": "from passlib.hash import bcrypt; print(bcrypt.using(rounds=4).hash('x'))",
    "import": "passlib",
    "pypi": "https://pypi.org/project/passlib/"
  },
  "full_name": "passlib",
  "id": "pypi:passlib",
  "kind": "python_library",
  "metadata": {
    "category": "crypto",
    "entry": "passlib",
    "example": "from passlib.hash import bcrypt; print(bcrypt.using(rounds=4).hash('x'))",
    "import_name": "passlib",
    "pip_name": "passlib",
    "version": "1.7.4"
  },
  "metadata_status": "indexed",
  "name": "passlib",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "Password hashing library (bcrypt, scrypt, pbkdf2, ...).",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/passlib/",
  "tags": [
    "offensive",
    "passlib",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/passlib && python3 passlib.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/passlib && python3 passlib.py --help",
    "cd toolboxes/passlib && python3 passlib.py --help",
    "cd toolboxes/passlib && python3 passlib.py --help",
    "cd toolboxes/passlib && python3 passlib.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST passlib --output $KFIOSA_OUTPUT_DIR/passlib.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST passlib --output $KFIOSA_OUTPUT_DIR/passlib.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}