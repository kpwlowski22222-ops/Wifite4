{
  "category": "exploit",
  "description": "XSStrike is an advanced XSS detection suite. It features a powerful fuzzing engine and an intelligent payload generator.",
  "documentation": {
    "entry": "xsstrike",
    "example": "python xsstrike.py -u 'https://example.com/?q=test'",
    "import": "xsstrike",
    "pypi": "https://pypi.org/project/XSStrike/"
  },
  "full_name": "xsstrike",
  "id": "pypi:xsstrike",
  "kind": "python_library",
  "metadata": {
    "category": "exploit",
    "entry": "xsstrike",
    "example": "python xsstrike.py -u 'https://example.com/?q=test'",
    "import_name": "xsstrike",
    "pip_name": "XSStrike",
    "version": "0.0.4"
  },
  "metadata_status": "indexed",
  "name": "XSStrike",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": false,
    "level": "high",
    "requires_explicit_authorization": true,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "Advanced XSS detection suite (fuzzing, crawler, ...).",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/XSStrike/",
  "tags": [
    "offensive",
    "xsstrike",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/XSStrike && python3 XSStrike.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/XSStrike && python3 XSStrike.py --help",
    "cd toolboxes/XSStrike && python3 XSStrike.py --help",
    "cd toolboxes/XSStrike && python3 XSStrike.py --help",
    "cd toolboxes/XSStrike && python3 XSStrike.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST XSStrike --output $KFIOSA_OUTPUT_DIR/XSStrike.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST XSStrike --output $KFIOSA_OUTPUT_DIR/XSStrike.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}