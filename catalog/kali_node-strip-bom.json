{
  "id": "kali:node-strip-bom",
  "kind": "kali_source_package",
  "name": "node-strip-bom",
  "title": "node-strip-bom",
  "summary": "Strip UTF-8 byte order mark (BOM) from a string",
  "description": "Strip UTF-8 byte order mark (BOM) from a string\nNode module to strip UTF-8 byte order mark (BOM) from a string\n.\nNode.js is an event-based server-side JavaScript engine.",
  "version": "4.0.0-2",
  "architectures": [
    "all"
  ],
  "homepage": "https://github.com/sindresorhus/strip-bom",
  "repository": "https://github.com/sindresorhus/strip-bom",
  "source_package": "node-strip-bom",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install node-strip-bom"
  },
  "packages": [
    {
      "name": "node-strip-bom"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use node-strip-bom because Strip UTF-8 byte order mark (BOM) from a string.",
    "When the engagement is in the install phase, use node-strip-bom as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair node-strip-bom with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `node-strip-bom --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Strip UTF-8 byte order mark (BOM) from a string\nNode module to strip UTF-8 byte order mark (BOM) from a string\n.\nNode.js is an event-based server-side JavaScript engine.",
    "how_to_use": "Install/setup: sudo apt install node-strip-bom. Strip UTF-8 byte order mark (BOM) from a string.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Strip UTF-8 byte order mark (BOM) from a string.",
    "why_to_use": "Prefer this tool based on upstream description: Strip UTF-8 byte order mark (BOM) from a string\nNode module to strip UTF-8 byte order mark (BOM) from a string\n.\nNode.js is an event-based server-side JavaScript engine.",
    "usage_sections": [
      "Install with: sudo apt install node-strip-bom",
      "Install with: sudo apt install node-strip-bom"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
