{
  "category": "utility",
  "description": "Pillow is the friendly PIL fork by Alex Clark and contributors. Adds image processing capabilities to your Python interpreter.",
  "documentation": {
    "entry": "PIL",
    "example": "from PIL import Image; print(Image.new('RGB', (10, 10)).size)",
    "import": "PIL",
    "pypi": "https://pypi.org/project/pillow/"
  },
  "full_name": "PIL",
  "id": "pypi:pillow",
  "kind": "python_library",
  "metadata": {
    "category": "utility",
    "entry": "PIL",
    "example": "from PIL import Image; print(Image.new('RGB', (10, 10)).size)",
    "import_name": "PIL",
    "pip_name": "pillow",
    "version": "10.4.0"
  },
  "metadata_status": "indexed",
  "name": "pillow",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "PIL fork (image processing).",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/pillow/",
  "tags": [
    "offensive",
    "pillow",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/pillow && python3 pillow.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/pillow && python3 pillow.py --help",
    "cd toolboxes/pillow && python3 pillow.py --help",
    "cd toolboxes/pillow && python3 pillow.py --help",
    "cd toolboxes/pillow && python3 pillow.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST pillow --output $KFIOSA_OUTPUT_DIR/pillow.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST pillow --output $KFIOSA_OUTPUT_DIR/pillow.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}