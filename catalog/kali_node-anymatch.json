{
  "id": "kali:node-anymatch",
  "kind": "kali_source_package",
  "name": "node-anymatch",
  "title": "node-anymatch",
  "summary": "Matches strings against configurable strings",
  "description": "Matches strings against configurable strings\nJavascript module to match a string against a regular expression, glob, string,\nor function that takes the string as an argument and returns a truthy or falsy\nvalue. The matcher can also be an array of any or all of these. Useful for\nallowing a very flexible user-defined config to define things like file paths.\n.\nIt is a dependency for ava, a futuristic test-runner\n.\nNode.js is an event-based server-side JavaScript engine.",
  "version": "3.1.3+~cs8.0.6-1",
  "architectures": [
    "all"
  ],
  "homepage": "https://github.com/es128/anymatch",
  "repository": "https://github.com/es128/anymatch",
  "source_package": "node-anymatch",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install node-anymatch"
  },
  "packages": [
    {
      "name": "node-anymatch"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use node-anymatch because Matches strings against configurable strings.",
    "When the engagement is in the install phase, use node-anymatch as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair node-anymatch with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `node-anymatch --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Matches strings against configurable strings\nJavascript module to match a string against a regular expression, glob, string,\nor function that takes the string as an argument and returns a truthy or falsy\nvalue. The matcher can also be an array of any or all of these. Useful for\nallowing a very flexible user-defined config to define things like file paths.\n.\nIt is a dependency for ava, a futuristic test-runner\n.\nNode.js is an event-based server-side JavaScript engine.",
    "how_to_use": "Install/setup: sudo apt install node-anymatch. Matches strings against configurable strings.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Matches strings against configurable strings.",
    "why_to_use": "Prefer this tool based on upstream description: Matches strings against configurable strings\nJavascript module to match a string against a regular expression, glob, string,\nor function that takes the string as an argument and returns a truthy or falsy\nvalue. The matcher can also be an array of any or all of these. Useful for\nallowing a very flexible user-defined config to define things like file paths.\n.\nIt is a dependency for ava, a futuristic.",
    "usage_sections": [
      "Install with: sudo apt install node-anymatch",
      "Install with: sudo apt install node-anymatch"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
