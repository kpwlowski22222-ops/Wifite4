{
  "category": "network",
  "description": "Twisted is an event-driven networking engine written in Python. It includes a web server, multiple protocols, clients, servers, and a flexible channel abstraction.",
  "documentation": {
    "entry": "twisted",
    "example": "from twisted.internet import reactor",
    "import": "twisted",
    "pypi": "https://pypi.org/project/twisted/"
  },
  "full_name": "twisted",
  "id": "pypi:twisted",
  "kind": "python_library",
  "metadata": {
    "category": "network",
    "entry": "twisted",
    "example": "from twisted.internet import reactor",
    "import_name": "twisted",
    "pip_name": "twisted",
    "version": "24.7.0"
  },
  "metadata_status": "indexed",
  "name": "twisted",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": false,
    "level": "medium",
    "requires_explicit_authorization": true,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "Asynchronous networking framework.",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/twisted/",
  "tags": [
    "offensive",
    "twisted",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/twisted && python3 twisted.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/twisted && python3 twisted.py --help",
    "cd toolboxes/twisted && python3 twisted.py --help",
    "cd toolboxes/twisted && python3 twisted.py --help",
    "cd toolboxes/twisted && python3 twisted.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST twisted --output $KFIOSA_OUTPUT_DIR/twisted.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST twisted --output $KFIOSA_OUTPUT_DIR/twisted.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}