{
  "id": "kali:cron-daemon-common",
  "kind": "kali_source_package",
  "name": "cron-daemon-common",
  "title": "cron-daemon-common",
  "summary": "process scheduling daemon's configuration files",
  "description": "process scheduling daemon's configuration files\nThe cron daemon is a background process that runs particular programs at\nparticular times (for example, every minute, day, week, or month), as\nspecified in a crontab. By default, users may also create crontabs of\ntheir own so that processes are run on their behalf.\n.\nThis package provides configuration files which must be there to\ndefine scheduled process sets.",
  "version": "3.0pl1-209",
  "architectures": [
    "all"
  ],
  "homepage": "https://ftp.isc.org/isc/cron/",
  "repository": "https://ftp.isc.org/isc/cron/",
  "source_package": "cron",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install cron-daemon-common"
  },
  "packages": [
    {
      "name": "cron-daemon-common"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use cron-daemon-common because process scheduling daemon's configuration files.",
    "When the engagement is in the install phase, use cron-daemon-common as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair cron-daemon-common with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `cron-daemon-common --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "process scheduling daemon's configuration files\nThe cron daemon is a background process that runs particular programs at\nparticular times (for example, every minute, day, week, or month), as\nspecified in a crontab. By default, users may also create crontabs of\ntheir own so that processes are run on their behalf.\n.\nThis package provides configuration files which must be there to\ndefine scheduled process sets.",
    "how_to_use": "Install/setup: sudo apt install cron. process scheduling daemon's configuration files.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: process scheduling daemon's configuration files.",
    "why_to_use": "Prefer this tool based on upstream description: process scheduling daemon's configuration files\nThe cron daemon is a background process that runs particular programs at\nparticular times (for example, every minute, day, week, or month), as\nspecified in a crontab. By default, users may also create crontabs of\ntheir own so that processes are run on their behalf.\n.\nThis package provides configuration files which must be there to\ndefine scheduled pr.",
    "usage_sections": [
      "Install with: sudo apt install cron",
      "Install with: sudo apt install cron"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
