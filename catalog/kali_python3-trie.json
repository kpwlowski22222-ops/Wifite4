{
  "id": "kali:python3-trie",
  "kind": "kali_source_package",
  "name": "python3-trie",
  "title": "python3-trie",
  "summary": "Pure Python implementation of the trie data structure (Python 3)",
  "description": "Pure Python implementation of the trie data structure (Python 3)\nA trie is an ordered tree data structure that is used to store a mapping where\nthe keys are sequences, usually strings over an alphabet.\nIn addition to implementing the mapping interface, tries allow finding the\nitems for a given prefix, and vice versa, finding the items whose keys are\nprefixes of a given key.\n.\nThis is the Python 3 package.",
  "version": "0.4.0+ds-1",
  "architectures": [
    "all"
  ],
  "homepage": "https://github.com/gsakkis/pytrie",
  "repository": "https://github.com/gsakkis/pytrie",
  "source_package": "python-trie",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install python3-trie"
  },
  "packages": [
    {
      "name": "python3-trie"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package",
    "python"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use python3-trie because Pure Python implementation of the trie data structure (Python 3).",
    "When the engagement is in the install phase, use python3-trie as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair python3-trie with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `python3-trie --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Pure Python implementation of the trie data structure (Python 3)\nA trie is an ordered tree data structure that is used to store a mapping where\nthe keys are sequences, usually strings over an alphabet.\nIn addition to implementing the mapping interface, tries allow finding the\nitems for a given prefix, and vice versa, finding the items whose keys are\nprefixes of a given key.\n.\nThis is the Python 3 package.",
    "how_to_use": "Install/setup: sudo apt install python-trie. Pure Python implementation of the trie data structure (Python 3).",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Pure Python implementation of the trie data structure (Python 3).",
    "why_to_use": "Prefer this tool based on upstream description: Pure Python implementation of the trie data structure (Python 3)\nA trie is an ordered tree data structure that is used to store a mapping where\nthe keys are sequences, usually strings over an alphabet.\nIn addition to implementing the mapping interface, tries allow finding the\nitems for a given prefix, and vice versa, finding the items whose keys are\nprefixes of a given key.\n.\nThis is the Python 3 .",
    "usage_sections": [
      "Install with: sudo apt install python-trie",
      "Install with: sudo apt install python-trie"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
