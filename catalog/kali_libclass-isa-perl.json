{
  "id": "kali:libclass-isa-perl",
  "kind": "kali_source_package",
  "name": "libclass-isa-perl",
  "title": "libclass-isa-perl",
  "summary": "report the search path for a class's ISA tree",
  "description": "report the search path for a class's ISA tree\nClass::ISA returns the list of classes that the current class inherits\nfrom, listed in the order that Perl would search for a method not\nimplemented in the current class. The list contains no duplicates.",
  "version": "0.36-8",
  "architectures": [
    "all"
  ],
  "homepage": "https://metacpan.org/release/Class-ISA",
  "repository": "https://metacpan.org/release/Class-ISA",
  "source_package": "libclass-isa-perl",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libclass-isa-perl"
  },
  "packages": [
    {
      "name": "libclass-isa-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/Class::ISA.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libclass-isa-perl because report the search path for a class's ISA tree.",
    "When the engagement is in the install phase, use libclass-isa-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libclass-isa-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libclass-isa-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "report the search path for a class's ISA tree\nClass::ISA returns the list of classes that the current class inherits\nfrom, listed in the order that Perl would search for a method not\nimplemented in the current class. The list contains no duplicates.",
    "how_to_use": "Install/setup: sudo apt install libclass-isa-perl. report the search path for a class's ISA tree.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: report the search path for a class's ISA tree.",
    "why_to_use": "Prefer this tool based on upstream description: report the search path for a class's ISA tree\nClass::ISA returns the list of classes that the current class inherits\nfrom, listed in the order that Perl would search for a method not\nimplemented in the current class. The list contains no duplicates.",
    "usage_sections": [
      "Install with: sudo apt install libclass-isa-perl",
      "Install with: sudo apt install libclass-isa-perl"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
