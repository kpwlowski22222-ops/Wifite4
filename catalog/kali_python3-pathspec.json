{
  "id": "kali:python3-pathspec",
  "kind": "kali_source_package",
  "name": "python3-pathspec",
  "title": "python3-pathspec",
  "summary": "utility library for gitignore style pattern matching of file paths (Python 3)",
  "description": "utility library for gitignore style pattern matching of file paths (Python 3)\npathspec is a utility library for pattern matching of file paths.\nSo far this only includes Git's wildmatch pattern matching which itself is\nderived from Rsync's wildmatch. Git uses wildmatch for its gitignore files.\n.\nThis package installs the library for Python 3.",
  "version": "1.0.4-1",
  "architectures": [
    "all"
  ],
  "homepage": "https://github.com/cpburnz/python-pathspec",
  "repository": "https://github.com/cpburnz/python-pathspec",
  "source_package": "python-pathspec",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install python3-pathspec"
  },
  "packages": [
    {
      "name": "python3-pathspec"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package",
    "python"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use python3-pathspec because utility library for gitignore style pattern matching of file paths (Python 3).",
    "When the engagement is in the install phase, use python3-pathspec as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair python3-pathspec with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `python3-pathspec --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "utility library for gitignore style pattern matching of file paths (Python 3)\npathspec is a utility library for pattern matching of file paths.\nSo far this only includes Git's wildmatch pattern matching which itself is\nderived from Rsync's wildmatch. Git uses wildmatch for its gitignore files.\n.\nThis package installs the library for Python 3.",
    "how_to_use": "Install/setup: sudo apt install python-pathspec. utility library for gitignore style pattern matching of file paths (Python 3).",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: utility library for gitignore style pattern matching of file paths (Python 3).",
    "why_to_use": "Prefer this tool based on upstream description: utility library for gitignore style pattern matching of file paths (Python 3)\npathspec is a utility library for pattern matching of file paths.\nSo far this only includes Git's wildmatch pattern matching which itself is\nderived from Rsync's wildmatch. Git uses wildmatch for its gitignore files.\n.\nThis package installs the library for Python 3.",
    "usage_sections": [
      "Install with: sudo apt install python-pathspec",
      "Install with: sudo apt install python-pathspec"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
