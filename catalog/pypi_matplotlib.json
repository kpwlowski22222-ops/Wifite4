{
  "category": "ai",
  "description": "Matplotlib is a comprehensive library for creating static, animated, and interactive visualizations in Python.",
  "documentation": {
    "entry": "matplotlib",
    "example": "import matplotlib; print(matplotlib.__version__)",
    "import": "matplotlib",
    "pypi": "https://pypi.org/project/matplotlib/"
  },
  "full_name": "matplotlib",
  "id": "pypi:matplotlib",
  "kind": "python_library",
  "metadata": {
    "category": "ai",
    "entry": "matplotlib",
    "example": "import matplotlib; print(matplotlib.__version__)",
    "import_name": "matplotlib",
    "pip_name": "matplotlib",
    "version": "3.9.0"
  },
  "metadata_status": "indexed",
  "name": "matplotlib",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "Plotting library (static, animated, interactive).",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/matplotlib/",
  "tags": [
    "offensive",
    "matplotlib",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/matplotlib && python3 matplotlib.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/matplotlib && python3 matplotlib.py --help",
    "cd toolboxes/matplotlib && python3 matplotlib.py --help",
    "cd toolboxes/matplotlib && python3 matplotlib.py --help",
    "cd toolboxes/matplotlib && python3 matplotlib.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST matplotlib --output $KFIOSA_OUTPUT_DIR/matplotlib.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST matplotlib --output $KFIOSA_OUTPUT_DIR/matplotlib.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}