{
  "id": "kali:libxml-simple-perl",
  "kind": "kali_source_package",
  "name": "libxml-simple-perl",
  "title": "libxml-simple-perl",
  "summary": "Perl module for reading and writing XML",
  "description": "Perl module for reading and writing XML\nThe XML::Simple module provides a simple API layer on top of an underlying\nXML parsing module to maintain XML files (especially configuration\nfiles).  Two functions are exported: XMLin() and XMLout().\n.\nThe simplest approach is to call these two functions directly, but an\noptional object oriented interface allows them to be called as\nmethods of an XML::Simple object.  The object interface can also be\nused at either end of a SAX pipeline.\n.\nNote from upstream: The use of this module in new code is strongly\ndiscouraged. Other modules are available which provide more straightforward\nand consistent interfaces. In particular, XML::LibXML is highly recommended.\nXML::Twig is another excellent alternative.These modules are packaged as\nlibxml-libxml-perl and libxml-twig-perl in Debian.",
  "version": "2.25-2",
  "architectures": [
    "all"
  ],
  "homepage": "https://metacpan.org/release/XML-Simple",
  "repository": "https://metacpan.org/release/XML-Simple",
  "source_package": "libxml-simple-perl",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libxml-simple-perl"
  },
  "packages": [
    {
      "name": "libxml-simple-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/XML::Simple.3pm.gz",
    "/usr/share/man/man3/XML::Simple::FAQ.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libxml-simple-perl because Perl module for reading and writing XML.",
    "When the engagement is in the install phase, use libxml-simple-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libxml-simple-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libxml-simple-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Perl module for reading and writing XML\nThe XML::Simple module provides a simple API layer on top of an underlying\nXML parsing module to maintain XML files (especially configuration\nfiles).  Two functions are exported: XMLin() and XMLout().\n.\nThe simplest approach is to call these two functions directly, but an\noptional object oriented interface allows them to be called as\nmethods of an XML::Simple object.  The object interface can also be\nused at either end of a SAX pipeline.\n.\nNote from upstream: The use of this module in new code is strongly\ndiscouraged. Other modules are available which provide more straightforward\nand consistent interfaces. In particular, XML::LibXML is highly recommended.\nXML::Twig is another excellent alternative.These modules are packaged as\nlibxml-libxml-perl and libxml-twig-perl in Debian.",
    "how_to_use": "Install/setup: sudo apt install libxml-simple-perl.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Perl module for reading and writing XML.",
    "why_to_use": "Prefer this tool based on upstream description: Perl module for reading and writing XML\nThe XML::Simple module provides a simple API layer on top of an underlying\nXML parsing module to maintain XML files (especially configuration\nfiles).  Two functions are exported: XMLin() and XMLout().\n.\nThe simplest approach is to call these two functions directly, but an\noptional object oriented interface allows them to be called as\nmethods of an XML::Simpl.",
    "usage_sections": [
      "Install with: sudo apt install libxml-simple-perl",
      "Install with: sudo apt install libxml-simple-perl"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
