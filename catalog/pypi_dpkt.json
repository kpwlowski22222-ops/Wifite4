{
  "category": "network",
  "description": "dpkt is a Python library for fast, simple packet creation / parsing, with definitions for TCP, UDP, IP, ICMP, Ethernet, ARP, IPv6, BGP, OSPF, RIP, DNS, etc.",
  "documentation": {
    "entry": "dpkt",
    "example": "import dpkt, socket; pcap = dpkt.pcap.Reader(open('cap.pcap', 'rb')); [print(socket.inet_ntoa(p[1].dst)) for _, p in pcap]",
    "import": "dpkt",
    "pypi": "https://pypi.org/project/dpkt/",
    "how_to_use": "Run the primary entrypoint as shown in upstream docs, e.g. `cd toolboxes/dpkt && python3 dpkt.py --target $KFIOSA_TARGET_HOST`. Further invocations from upstream: `cd toolboxes/dpkt && python3 dpkt.py --help`; `cd toolboxes/dpkt && python3 dpkt.py --help`. Fast, simple packet creation / parsing, with definitions for TCP, UDP, IP, ICMP, Ethernet, ARP, .",
    "when_to_use": "Use during the 'any' phase against surfaces [web] in the network category. Fits when the engagement needs: Fast, simple packet creation / parsing, with definitions for TCP, UDP, IP, ICMP, Ethernet, ARP, .",
    "why_to_use": "Prefer this tool because: Fast, simple packet creation / parsing, with definitions for TCP, UDP, IP, ICMP, Ethernet, ARP, ."
  },
  "full_name": "dpkt",
  "id": "pypi:dpkt",
  "kind": "python_library",
  "metadata": {
    "category": "network",
    "entry": "dpkt",
    "example": "import dpkt, socket; pcap = dpkt.pcap.Reader(open('cap.pcap', 'rb')); [print(socket.inet_ntoa(p[1].dst)) for _, p in pcap]",
    "import_name": "dpkt",
    "pip_name": "dpkt",
    "version": "1.9.8"
  },
  "metadata_status": "indexed",
  "name": "dpkt",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "Fast, simple packet creation / parsing, with definitions for TCP, UDP, IP, ICMP, Ethernet, ARP, ...",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/dpkt/",
  "tags": [
    "offensive",
    "dpkt",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/dpkt && python3 dpkt.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/dpkt && python3 dpkt.py --help",
    "cd toolboxes/dpkt && python3 dpkt.py --help",
    "cd toolboxes/dpkt && python3 dpkt.py --help",
    "cd toolboxes/dpkt && python3 dpkt.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST dpkt --output $KFIOSA_OUTPUT_DIR/dpkt.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST dpkt --output $KFIOSA_OUTPUT_DIR/dpkt.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0",
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline"
}
