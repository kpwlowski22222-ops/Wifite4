{
  "category": "exploit",
  "description": "pyotp is a Python library for generating and parsing one-time passwords. Supports HOTP, TOTP, and Steam guard codes.",
  "documentation": {
    "entry": "pyotp",
    "example": "import pyotp; print(pyotp.TOTP('JBSWY3DPEHPK3PXP').now())",
    "import": "pyotp",
    "pypi": "https://pypi.org/project/pyotp/"
  },
  "full_name": "pyotp",
  "id": "pypi:pyotp",
  "kind": "python_library",
  "metadata": {
    "category": "exploit",
    "entry": "pyotp",
    "example": "import pyotp; print(pyotp.TOTP('JBSWY3DPEHPK3PXP').now())",
    "import_name": "pyotp",
    "pip_name": "pyotp",
    "version": "2.9.0"
  },
  "metadata_status": "indexed",
  "name": "pyotp",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": true,
    "level": "low",
    "requires_explicit_authorization": false,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "TOTP / HOTP implementation.",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/pyotp/",
  "tags": [
    "offensive",
    "pyotp",
    "web",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/pyotp && python3 pyotp.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/pyotp && python3 pyotp.py --help",
    "cd toolboxes/pyotp && python3 pyotp.py --help",
    "cd toolboxes/pyotp && python3 pyotp.py --help",
    "cd toolboxes/pyotp && python3 pyotp.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST pyotp --output $KFIOSA_OUTPUT_DIR/pyotp.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST pyotp --output $KFIOSA_OUTPUT_DIR/pyotp.log"
  ],
  "attack_surface": [
    "web"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}