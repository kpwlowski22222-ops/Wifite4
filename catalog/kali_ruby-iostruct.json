{
  "id": "kali:ruby-iostruct",
  "kind": "kali_source_package",
  "name": "ruby-iostruct",
  "title": "ruby-iostruct",
  "summary": "Struct that can read/write itself from/to IO-like objects",
  "description": "Struct that can read/write itself from/to IO-like objects\nThis package contains a gem to manage a struct that can read/write itself\nfrom/to IO-like objects.",
  "version": "0.0.4-0kali1",
  "architectures": [
    "all"
  ],
  "homepage": "http://github.com/zed-0xff/iostruct",
  "repository": "http://github.com/zed-0xff/iostruct",
  "source_package": "ruby-iostruct",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install ruby-iostruct"
  },
  "packages": [
    {
      "name": "ruby-iostruct"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use ruby-iostruct because Struct that can read/write itself from/to IO-like objects.",
    "When the engagement is in the install phase, use ruby-iostruct as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair ruby-iostruct with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `ruby-iostruct --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "Struct that can read/write itself from/to IO-like objects\nThis package contains a gem to manage a struct that can read/write itself\nfrom/to IO-like objects.",
    "how_to_use": "Install/setup: sudo apt install ruby-iostruct. Struct that can read/write itself from/to IO-like objects.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: Struct that can read/write itself from/to IO-like objects.",
    "why_to_use": "Prefer this tool based on upstream description: Struct that can read/write itself from/to IO-like objects\nThis package contains a gem to manage a struct that can read/write itself\nfrom/to IO-like objects.",
    "usage_sections": [
      "Install with: sudo apt install ruby-iostruct",
      "Install with: sudo apt install ruby-iostruct"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
