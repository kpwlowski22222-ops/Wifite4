{
  "id": "kali:python3-fsspec",
  "kind": "kali_source_package",
  "name": "python3-fsspec",
  "title": "python3-fsspec",
  "summary": "specification that Python filesystems should adhere to (Python 3)",
  "description": "specification that Python filesystems should adhere to (Python 3)\nThe package produce a template or specification for a file-system interface,\nthat specific implementations should follow, so that applications making use\nof them can rely on a common behaviour and not have to worry about the\nspecific internal implementation decisions with any given backend.",
  "version": "2025.12.0-1",
  "architectures": [
    "all"
  ],
  "homepage": "https://github.com/fsspec/filesystem_spec",
  "repository": "https://github.com/fsspec/filesystem_spec",
  "source_package": "fsspec",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install python3-fsspec"
  },
  "packages": [
    {
      "name": "python3-fsspec"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package",
    "python"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use python3-fsspec because specification that Python filesystems should adhere to (Python 3).",
    "When the engagement is in the install phase, use python3-fsspec as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair python3-fsspec with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `python3-fsspec --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "specification that Python filesystems should adhere to (Python 3)\nThe package produce a template or specification for a file-system interface,\nthat specific implementations should follow, so that applications making use\nof them can rely on a common behaviour and not have to worry about the\nspecific internal implementation decisions with any given backend.",
    "how_to_use": "Install/setup: sudo apt install fsspec. specification that Python filesystems should adhere to (Python 3).",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: specification that Python filesystems should adhere to (Python 3).",
    "why_to_use": "Prefer this tool based on upstream description: specification that Python filesystems should adhere to (Python 3)\nThe package produce a template or specification for a file-system interface,\nthat specific implementations should follow, so that applications making use\nof them can rely on a common behaviour and not have to worry about the\nspecific internal implementation decisions with any given backend.",
    "usage_sections": [
      "Install with: sudo apt install fsspec",
      "Install with: sudo apt install fsspec"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
