{
  "id": "kali:dnsmap",
  "kind": "kali_source_package",
  "name": "dnsmap",
  "title": "dnsmap",
  "summary": "DNS domain name brute forcing tool",
  "description": "DNS domain name brute forcing tool\ndnsmap scans a domain for common subdomains using a built-in or an external\nwordlist (if specified using -w option). The internal wordlist has around 1000\nwords in English and Spanish as ns1, firewall servicios and smtp. So will be\npossible search for smtp.example.com inside example.com automatically. Results\ncan be saved in CSV and human-readable format for further processing. dnsmap\ndoes NOT require root privileges to be run, and should NOT be run with such\nprivileges for security reasons.\n.\ndnsmap was originally released back in 2006 and was inspired by the fictional\nstory \"The Thief No One Saw\" by Paul Craig, which can be found in the book\n\"Stealing the Network - How to 0wn the Box\".\n.\ndnsmap is mainly meant to be used by pentesters during the information\ngathering/enumeration phase of infrastructure security assessments. During the\nenumeration stage, the security consultant would typically discover the target\ncompany's IP netblocks, domain names, phone numbers, etc.\n.\nSubdomain brute-forcing is another technique that should be used in the\nenumeration stage, as it's especially useful when other domain enumeration\ntechniques such as zone transfers don't work (I rarely see zone transfers being\npublicly allowed these days by the way).\n.\nFun things that can happen:\n.\n1) Finding interesting remote access servers\n(e.g.: https:://extranet.example.com).\n2) Finding badly configured and/or unpatched servers\n(e.g.: test.example.com).\n3) Finding new domain names which will allow you to map\nnon-obvious/hard-to-find netblocks of your target organization\n(registry lookups - aka whois is your friend).\n4) Sometimes you find that some bruteforced subdomains resolve to internal IP\naddresses (RFC 1918). This is great as sometimes they are real up-to-date\n\"A\" records which means that it is possible to enumerate internal servers\nof a target organization from the Internet by only using standard DNS\nresolving (as opposed to zone transfers for instance).\n5) Discover embedded devices configured using Dynamic DNS services\n(e.g.: IP Cameras). This method is an alternative to finding devices via\nGoogle hacking techniques.\n.\nThis package provides two possible commands: dnsmap and dnsmap-bulk.\n.\nThis program is useful for pentesters, ethical hackers and forensics experts.\nIt also can be used for security tests.",
  "version": "0.36-4",
  "architectures": [
    "amd64"
  ],
  "homepage": "https://github.com/resurrecting-open-source-projects/dnsmap",
  "repository": "https://github.com/resurrecting-open-source-projects/dnsmap",
  "source_package": "dnsmap",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install dnsmap"
  },
  "packages": [
    {
      "name": "dnsmap"
    }
  ],
  "commands": [
    {
      "name": "dnsmap",
      "path": "/usr/bin/dnsmap",
      "description": "scan for subdomains using bruteforcing techniques",
      "usage": "dnsmap [options] [args]",
      "arguments": [
        {
          "flags": [
            "-c"
          ],
          "metavar": null,
          "takes_value": false,
          "description": "Save results in CSV format in a file. If a file name isn't provided, dnsmap will create",
          "default": null,
          "choices": null,
          "raw": "-c  Save results in CSV format in a file. If a file name isn't provided, dnsmap will create"
        },
        {
          "flags": [
            "-d"
          ],
          "metavar": null,
          "takes_value": false,
          "description": "Limit of random delay in milliseconds between successive queries. Delay value is a max-",
          "default": null,
          "choices": null,
          "raw": "-d  Limit of random delay in milliseconds between successive queries. Delay value is a max-"
        },
        {
          "flags": [
            "-i"
          ],
          "metavar": null,
          "takes_value": false,
          "description": "IP addresses to ignore in the results (useful if you get obtaining false positives).",
          "default": null,
          "choices": null,
          "raw": "-i  IP addresses to ignore in the results (useful if you get obtaining false positives)."
        },
        {
          "flags": [
            "-r"
          ],
          "metavar": null,
          "takes_value": false,
          "description": "Save results to a plain text file. If a file name isn't supplied, dnsmap will create an",
          "default": null,
          "choices": null,
          "raw": "-r  Save results to a plain text file. If a file name isn't supplied, dnsmap will create an"
        },
        {
          "flags": [
            "-w"
          ],
          "metavar": null,
          "takes_value": false,
          "description": "Use an external wordlist instead of the built-in one. You can use programs as crunch or",
          "default": null,
          "choices": null,
          "raw": "-w  Use an external wordlist instead of the built-in one. You can use programs as crunch or"
        }
      ]
    },
    {
      "name": "dnsmap-bulk",
      "path": "/usr/bin/dnsmap-bulk",
      "description": "mass scan using dnsmap",
      "usage": "dnsmap-bulk [options] [args]",
      "arguments": []
    }
  ],
  "files": [
    "/usr/bin/dnsmap",
    "/usr/bin/dnsmap-bulk",
    "/usr/share/man/man1/dnsmap-bulk.1.gz",
    "/usr/share/man/man1/dnsmap.1.gz"
  ],
  "tags": [
    "os-package",
    "networking"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "command_examples": [
    "dnsmap <target-domain> [options]",
    "dnsmap example.com",
    "dnsmap example.com -w wordlist.txt",
    "dnsmap example.com -r /tmp",
    "dnsmap example.com -r /tmp/ -d 300",
    "dnsmap example.com -d 800 -r /tmp/ -c /tmp/ -i $KFIOSA_TARGET_HOST,$KFIOSA_TARGET_HOST -w ./wordlist_TLAs.txt"
  ],
  "use_cases": [
    "When you need Kali Linux, use dnsmap because DNS domain name brute forcing tool.",
    "When the engagement is in the install phase, use dnsmap as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When you need a concrete invocation, run `dnsmap <target-domain> [options]` as documented upstream for dnsmap.",
    "When you need a concrete invocation, run `dnsmap example.com` as documented upstream for dnsmap.",
    "When you need a concrete invocation, run `dnsmap example.com -w wordlist.txt` as documented upstream for dnsmap.",
    "When you need a concrete invocation, run `dnsmap example.com -r /tmp` as documented upstream for dnsmap.",
    "When chaining Kali Linux tools, pair dnsmap with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `dnsmap --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [
      {
        "name": "-c",
        "description": "Save results in CSV format in a file. If a file name isn't provided, dnsmap will create",
        "source": "docs"
      },
      {
        "name": "-d",
        "description": "Limit of random delay in milliseconds between successive queries. Delay value is a max-",
        "source": "docs"
      },
      {
        "name": "-i",
        "description": "IP addresses to ignore in the results (useful if you get obtaining false positives).",
        "source": "docs"
      },
      {
        "name": "-r",
        "description": "Save results to a plain text file. If a file name isn't supplied, dnsmap will create an",
        "source": "docs"
      },
      {
        "name": "-w",
        "description": "Use an external wordlist instead of the built-in one. You can use programs as crunch or",
        "source": "docs"
      }
    ],
    "examples": [
      "dnsmap <target-domain> [options]",
      "dnsmap example.com",
      "dnsmap example.com -w wordlist.txt",
      "dnsmap example.com -r /tmp",
      "dnsmap example.com -r /tmp/ -d 300",
      "dnsmap example.com -d 800 -r /tmp/ -c /tmp/ -i $KFIOSA_TARGET_HOST,$KFIOSA_TARGET_HOST -w ./wordlist_TLAs.txt"
    ],
    "readme": "dnsmap - scan for subdomains using bruteforcing techniques",
    "how_to_use": "Install/setup: sudo apt install dnsmap. Run the primary entrypoint as shown in upstream docs, e.g. `dnsmap <target-domain> [options]`. Further invocations from upstream: `dnsmap example.com`; `dnsmap example.com -w wordlist.txt`.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: DNS domain name brute forcing tool.",
    "why_to_use": "Prefer this tool based on upstream description: dnsmap - scan for subdomains using bruteforcing techniques.",
    "usage_sections": [
      "Install with: sudo apt install dnsmap",
      "Install with: sudo apt install dnsmap"
    ]
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "local_man"
}
