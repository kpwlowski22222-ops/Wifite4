{
  "id": "kali:python3-smmap",
  "kind": "kali_source_package",
  "name": "python3-smmap",
  "title": "python3-smmap",
  "summary": "pure Python implementation of a sliding window memory map manager",
  "description": "pure Python implementation of a sliding window memory map manager\nSmmap wraps an interface around mmap and tracks the mapped files as well\nas the amount of clients who use it. If the system runs out of resources,\nor if a memory limit is reached, it will automatically unload unused maps\nto allow continued operation.\n.\nThis package for Python 3.",
  "version": "6.0.0-1",
  "architectures": [
    "all"
  ],
  "homepage": "https://github.com/Byron/smmap",
  "repository": "https://github.com/Byron/smmap",
  "source_package": "python-smmap",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install python3-smmap"
  },
  "packages": [
    {
      "name": "python3-smmap"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package",
    "python"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use python3-smmap because pure Python implementation of a sliding window memory map manager.",
    "When the engagement is in the install phase, use python3-smmap as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair python3-smmap with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `python3-smmap --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "pure Python implementation of a sliding window memory map manager\nSmmap wraps an interface around mmap and tracks the mapped files as well\nas the amount of clients who use it. If the system runs out of resources,\nor if a memory limit is reached, it will automatically unload unused maps\nto allow continued operation.\n.\nThis package for Python 3.",
    "how_to_use": "Install/setup: sudo apt install python-smmap. pure Python implementation of a sliding window memory map manager.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: pure Python implementation of a sliding window memory map manager.",
    "why_to_use": "Prefer this tool based on upstream description: pure Python implementation of a sliding window memory map manager\nSmmap wraps an interface around mmap and tracks the mapped files as well\nas the amount of clients who use it. If the system runs out of resources,\nor if a memory limit is reached, it will automatically unload unused maps\nto allow continued operation.\n.\nThis package for Python 3.",
    "usage_sections": [
      "Install with: sudo apt install python-smmap",
      "Install with: sudo apt install python-smmap"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
