{
  "category": "ble",
  "description": "bleak is a cross-platform Bluetooth Low Energy (BLE) client library for Python. Supports Windows, Linux, macOS.",
  "documentation": {
    "entry": "bleak",
    "example": "import asyncio, bleak; asyncio.run(bleak.BleakScanner.discover())",
    "import": "bleak",
    "pypi": "https://pypi.org/project/bleak/"
  },
  "full_name": "bleak",
  "id": "pypi:bleak",
  "kind": "python_library",
  "metadata": {
    "category": "ble",
    "entry": "bleak",
    "example": "import asyncio, bleak; asyncio.run(bleak.BleakScanner.discover())",
    "import_name": "bleak",
    "pip_name": "bleak",
    "version": "0.21.3"
  },
  "metadata_status": "indexed",
  "name": "bleak",
  "owner": "pypi",
  "risk": {
    "allow_autonomous_execution": false,
    "level": "medium",
    "requires_explicit_authorization": true,
    "signals": [
      "offensive_tool",
      "intrinsic",
      "remote_possible",
      "polymorphic_compatible",
      "target_adaptive_compatible"
    ]
  },
  "summary": "Cross-platform BLE library (async).",
  "trust": {
    "notes": "Operator-curated Python library. The pip name is resolved from PyPI; the import name is verified against the registry.",
    "reviewed": true,
    "reviewer": "KFIOSA operator"
  },
  "url": "https://pypi.org/project/bleak/",
  "tags": [
    "offensive",
    "bleak",
    "web",
    "ble_4_x",
    "kfiosa",
    "indexed",
    "polymorphic",
    "target-adaptive"
  ],
  "use_cases": [
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Indexing only — no automated invocation. The operator must manually inspect the repository before adding to a toolchain.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Use as a candidate tool in the chain planner; review provenance before use.",
    "Pair with the LLM system-prompt stanza to ensure the chain planner knows how to call this tool."
  ],
  "command_examples": [
    "cd toolboxes/bleak && python3 bleak.py --target $KFIOSA_TARGET_HOST",
    "cd toolboxes/bleak && python3 bleak.py --help",
    "cd toolboxes/bleak && python3 bleak.py --help",
    "cd toolboxes/bleak && python3 bleak.py --help",
    "cd toolboxes/bleak && python3 bleak.py --help",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST bleak --output $KFIOSA_OUTPUT_DIR/bleak.log",
    "KFIOSA_TARGET_HOST=$KFIOSA_TARGET_HOST bleak --output $KFIOSA_OUTPUT_DIR/bleak.log"
  ],
  "attack_surface": [
    "web",
    "ble_4_x"
  ],
  "phase_hint": "any",
  "requires_hardware": [
    "none"
  ],
  "polymorphic_strategies": [
    "param_grammar"
  ],
  "target_adaptive_targets": [
    "target_class"
  ],
  "_kfiosa_enriched_schema": "1.1.0"
}