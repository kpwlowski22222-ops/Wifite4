{
  "id": "kali:libfile-stripnondeterminism-perl",
  "kind": "kali_source_package",
  "name": "libfile-stripnondeterminism-perl",
  "title": "libfile-stripnondeterminism-perl",
  "summary": "file non-deterministic information stripper — Perl module",
  "description": "file non-deterministic information stripper — Perl module\nStripNondeterminism is a library for stripping non-deterministic information\nsuch as timestamps and filesystem ordering from various file and archive\nformats.\n.\nThis can be used as a post-processing step to improve the reproducibility of a\nbuild product, when the build process itself cannot be made deterministic.\n.\nIt is used as part of the Reproducible Builds project, although it should be\nconsidered a temporary workaround which should not be needed in the long\nterm; upstream software should be reproducible even without using such a tool.\n.\nThis package installs the Perl module ‘File::StripNondeterminism’.",
  "version": "1.15.1-1",
  "architectures": [
    "all"
  ],
  "homepage": "https://reproducible-builds.org/",
  "repository": "https://reproducible-builds.org/",
  "source_package": "strip-nondeterminism",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libfile-stripnondeterminism-perl"
  },
  "packages": [
    {
      "name": "libfile-stripnondeterminism-perl"
    }
  ],
  "commands": [],
  "files": [],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libfile-stripnondeterminism-perl because file non-deterministic information stripper — Perl module.",
    "When the engagement is in the install phase, use libfile-stripnondeterminism-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libfile-stripnondeterminism-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libfile-stripnondeterminism-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "file non-deterministic information stripper — Perl module\nStripNondeterminism is a library for stripping non-deterministic information\nsuch as timestamps and filesystem ordering from various file and archive\nformats.\n.\nThis can be used as a post-processing step to improve the reproducibility of a\nbuild product, when the build process itself cannot be made deterministic.\n.\nIt is used as part of the Reproducible Builds project, although it should be\nconsidered a temporary workaround which should not be needed in the long\nterm; upstream software should be reproducible even without using such a tool.\n.\nThis package installs the Perl module ‘File::StripNondeterminism’.",
    "how_to_use": "Install/setup: sudo apt install strip-nondeterminism. file non-deterministic information stripper — Perl module.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: file non-deterministic information stripper — Perl module.",
    "why_to_use": "Prefer this tool based on upstream description: file non-deterministic information stripper — Perl module\nStripNondeterminism is a library for stripping non-deterministic information\nsuch as timestamps and filesystem ordering from various file and archive\nformats.\n.\nThis can be used as a post-processing step to improve the reproducibility of a\nbuild product, when the build process itself cannot be made deterministic.\n.\nIt is used as part of the.",
    "usage_sections": [
      "Install with: sudo apt install strip-nondeterminism",
      "Install with: sudo apt install strip-nondeterminism"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
