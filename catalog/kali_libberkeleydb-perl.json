{
  "id": "kali:libberkeleydb-perl",
  "kind": "kali_source_package",
  "name": "libberkeleydb-perl",
  "title": "libberkeleydb-perl",
  "summary": "use Berkeley DB databases from Perl",
  "description": "use Berkeley DB databases from Perl\nProvides BerkeleyDB::Hash, BerkeleyDB::Btree etc. classes that allow\naccess to Berkeley DB databases as if they were normal Perl hash\ntables.\n.\nSupports all the features of Berkeley DB such as sharing cache and\nlock memory between processes, logging and transactions.",
  "version": "0.66-2+b1",
  "architectures": [
    "amd64"
  ],
  "homepage": "https://github.com/pmqs/BerkeleyDB",
  "repository": "https://github.com/pmqs/BerkeleyDB",
  "source_package": "libberkeleydb-perl (0.66-2)",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libberkeleydb-perl"
  },
  "packages": [
    {
      "name": "libberkeleydb-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/BerkeleyDB.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libberkeleydb-perl because use Berkeley DB databases from Perl.",
    "When the engagement is in the install phase, use libberkeleydb-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libberkeleydb-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libberkeleydb-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "use Berkeley DB databases from Perl\nProvides BerkeleyDB::Hash, BerkeleyDB::Btree etc. classes that allow\naccess to Berkeley DB databases as if they were normal Perl hash\ntables.\n.\nSupports all the features of Berkeley DB such as sharing cache and\nlock memory between processes, logging and transactions.",
    "how_to_use": "Install/setup: sudo apt install libberkeleydb-perl (0.66-2).",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: use Berkeley DB databases from Perl.",
    "why_to_use": "Prefer this tool based on upstream description: use Berkeley DB databases from Perl\nProvides BerkeleyDB::Hash, BerkeleyDB::Btree etc. classes that allow\naccess to Berkeley DB databases as if they were normal Perl hash\ntables.\n.\nSupports all the features of Berkeley DB such as sharing cache and\nlock memory between processes, logging and transactions.",
    "usage_sections": [
      "Install with: sudo apt install libberkeleydb-perl (0.66-2)",
      "Install with: sudo apt install libberkeleydb-perl (0.66-2)"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
