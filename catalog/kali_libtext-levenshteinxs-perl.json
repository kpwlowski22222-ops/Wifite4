{
  "id": "kali:libtext-levenshteinxs-perl",
  "kind": "kali_source_package",
  "name": "libtext-levenshteinxs-perl",
  "title": "libtext-levenshteinxs-perl",
  "summary": "XS implementation of the Levenshtein edit distance",
  "description": "XS implementation of the Levenshtein edit distance\nText::LevenshteinXS implements the Levenshtein edit distance in a XS way; this\nshould be much faster than the pure Perl implementation.\n.\nThe Levenshtein edit distance is a measure of the degree of proximity between\ntwo strings. This distance is the number of substitutions, deletions or\ninsertions (\"edits\") needed to transform one string into the other one (and\nvice versa). When two strings have distance 0, they are the same.",
  "version": "0.03-5+b4",
  "architectures": [
    "amd64"
  ],
  "homepage": "https://metacpan.org/release/Text-LevenshteinXS",
  "repository": "https://metacpan.org/release/Text-LevenshteinXS",
  "source_package": "libtext-levenshteinxs-perl (0.03-5)",
  "metapackages": [],
  "install": {
    "apt": "sudo apt install libtext-levenshteinxs-perl"
  },
  "packages": [
    {
      "name": "libtext-levenshteinxs-perl"
    }
  ],
  "commands": [],
  "files": [
    "/usr/share/man/man3/Text::LevenshteinXS.3pm.gz"
  ],
  "tags": [
    "os-package",
    "library"
  ],
  "metadata_status": "os_generated",
  "schema_version": "1.1.0",
  "attack_surface": "local",
  "phase_hint": "install",
  "requires_hardware": false,
  "use_cases": [
    "When you need Kali Linux, use libtext-levenshteinxs-perl because XS implementation of the Levenshtein edit distance.",
    "When the engagement is in the install phase, use libtext-levenshteinxs-perl as a cataloged Kali Linux tool with operator ACCEPT/CANCEL gating.",
    "When chaining Kali Linux tools, pair libtext-levenshteinxs-perl with sibling catalog entries sharing its attack_surface/phase_hint.",
    "When validating lab setup, run `libtext-levenshteinxs-perl --help` (or the README entrypoint) and confirm required hardware/deps from the catalog."
  ],
  "documentation": {
    "arguments": [],
    "readme": "XS implementation of the Levenshtein edit distance\nText::LevenshteinXS implements the Levenshtein edit distance in a XS way; this\nshould be much faster than the pure Perl implementation.\n.\nThe Levenshtein edit distance is a measure of the degree of proximity between\ntwo strings. This distance is the number of substitutions, deletions or\ninsertions (\"edits\") needed to transform one string into the other one (and\nvice versa). When two strings have distance 0, they are the same.",
    "how_to_use": "Install/setup: sudo apt install libtext-levenshteinxs-perl (0.03-5). XS implementation of the Levenshtein edit distance.",
    "when_to_use": "Use during the 'install' phase against surfaces [local] in the Kali Linux category. Fits when the engagement needs: XS implementation of the Levenshtein edit distance.",
    "why_to_use": "Prefer this tool based on upstream description: XS implementation of the Levenshtein edit distance\nText::LevenshteinXS implements the Levenshtein edit distance in a XS way; this\nshould be much faster than the pure Perl implementation.\n.\nThe Levenshtein edit distance is a measure of the degree of proximity between\ntwo strings. This distance is the number of substitutions, deletions or\ninsertions (\"edits\") needed to transform one string into the .",
    "usage_sections": [
      "Install with: sudo apt install libtext-levenshteinxs-perl (0.03-5)",
      "Install with: sudo apt install libtext-levenshteinxs-perl (0.03-5)"
    ],
    "examples": []
  },
  "_kfiosa_enhanced_v3": true,
  "_enhanced_via": "offline",
  "command_examples": []
}
