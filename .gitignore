node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.fsc/
__pycache__/
*.pyc
*.pyo
# Skip nested .git dirs under toolboxes/ (they're cloned repos)
toolboxes/**/.git/
.claude/worktrees/

# Operator-machine noise — never commit (sensitive or session-only)
# Real Google OAuth client_secrets (never commit)
oauthadmin.json
client_secret_*.json
# Kismet live captures (operator's local WiFi traffic)
Kismet-*.kismet
Kismet-*.kismet-journal
# Session-continuation text files (harness-internal)
*this-session-is-being-continued*.txt
# Local session state
.first_run_complete
# Vendor / model / runtime caches
.blackboxcli/
.qwen/
.agents/
.venv/
# Stray CLI output (e.g. a `python main.py --help` redirect)
--help
--help.dsc
-h
# Workspace output (operator's local working dir, not source)
workspace/
output/
data/
datasets/
models/
# Cloned third-party tool sources (the catalog is the source of truth;
# the clones are vendored local copies, regenerated by the tool_installer)
toolboxes/
# Vendor / 3rd-party directories the operator added
kfiosa.egg-info/
qwen-code/
# kismet db recovery files
fake
# Plaintext OSINT sample inputs the operator's tooling generated
alice.txt
# Operation note files
github_exploit_repos.txt
# Pipeline shell wrappers (regenerable; not source)
debug_workspace.sh
run.sh
run_tui.sh
run_full_pipeline.sh
run_full_pipeline_fixed.sh
run_full_pipeline_v2.sh
run_nim_docker.sh
test_nim_local.sh
prepare.sh
prepare_ollama_finetune.sh
# Per-Phase design notes (regenerable; superseded by TODO.md / KNOWN_ISSUES.md)
implementacja.txt
implementacja_for.txt
implementacja_v2.txt
# Top-level python files that are operator-side entry points (not source)
ai_pentest_engine.py
metasploit_post_exploit.py
wifi_offensive_ai/
# google_cloud_integration.py was retired (replaced by
# core/integrations/shodan_integration.py and friends)
google_cloud_integration.py

