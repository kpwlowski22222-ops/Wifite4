# Google Cloud dependencies
google-cloud-aiplatform>=1.38.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-api-python-client>=2.108.0

# Additional utilities
requests>=2.31.0
python-dotenv>=1.0.0
# curses is in the stdlib on POSIX; on Windows install windows-curses
windows-curses>=2.1.0; platform_system == "Windows"

# AI/ML
torch>=2.0.0
transformers>=4.35.0
# Hugging Face datasets — Zero_Day dataset grounding for 0-day PoC gen
datasets>=2.16.0
huggingface_hub>=0.20.0

# Async HTTP (dashboard async scanners / Ollama streaming fallback)
aiohttp>=3.9.0

# BLE
bleak>=0.21.0

# OSINT
shodan>=1.31.0

# C2
dnslib>=0.9.24
pycryptodome>=3.19.0

# Metasploit
pymetasploit3>=1.0.4

# Optional: Hugging Face Hub for GGUF fetch (scripts/model_downloader.py)
huggingface_hub>=0.20.0

# Mobile instrumentation (Phase 3) — frida 15.x is the last line that
# supports Python 3.10 (16+ requires 3.11+ for typing.NotRequired).
# frida-tools 11.0.0 is the matching CLI. If you upgrade Python to
# 3.11+ these pins can be removed.
frida>=15.2.0,<16
frida-tools>=11.0.0,<12

# Multiple dispatch polymorphism (Python ≥3.10) — beartype/plum
# https://github.com/beartype/plum — typed target adaptation for AI/algorithms
plum-dispatch>=2.5.0
